# Copier ce fichier en .env pour le développement local (vercel dev le charge automatiquement).
# En production, définir cette variable dans les réglages du projet Vercel (Settings → Environment
# Variables) — jamais dans le code, jamais commitée.
ANTHROPIC_API_KEY=sk-ant-...
