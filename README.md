# Tarot de Delphes

Application web (PWA) de tarot mythologique grec — tirage interactif, lecture personnalisée générée par IA, bibliothèque de symboles, journal.

> Ce dépôt contient aussi, dans `enfants/`, une seconde application indépendante : **Mini Olympe**, destinée aux enfants (dès 6 ans) pour découvrir les dieux grecs par le jeu. Voir `enfants/README.md`.

## Structure
- `index.html` — structure de la page
- `styles.css` — tous les styles (police Cinzel/EB Garamond, thème "divinatoire", animation de chargement en étoiles)
- `app.js` — toute la logique (données des cartes, symboles, tirage, navigation, appel au backend IA)
- `assets/` — illustrations des 22 arcanes majeurs (Le Mat, Bateleur, Papesse, Impératrice, Empereur, Pape, Amoureux, Chariot, Justice, Hermite, Roue de Fortune, Force, Pendu, Arcane sans nom, Tempérance, Diable, Maison-Dieu, Étoile, Lune, Soleil, Jugement, Monde) — collection complète. Les 56 cartes mineures sont désormais toutes illustrées elles aussi : Épées, Bâtons et Coupes de longue date (10 numérales + 4 figures de cour chacun, `card-epees-*.jpg`, `card-batons-*.jpg` et `card-coupes-*.jpg`), et Deniers en dernier (`card-deniers-*.jpg`, fournies par l'utilisatrice en 3 lots successifs — As à 5, puis 6 à 9 et le Valet, puis le 10 et les 3 dernières figures de cour Cavalier/Reine/Roi). **Les 78 cartes du jeu sont donc désormais toutes illustrées.** Convention de nommage sur les bandeaux de titre : le nom de l'enseigne y reste toujours au singulier (« AS DE DENIER », « AS DE BÂTON », « AS DE COUPE »), jamais au pluriel — seules les 14 illustrations d'Épées y dérogeaient (« AS D'ÉPÉES » avec un S en trop) ; retour direct d'utilisatrice, d'abord corrigé directement sur chaque image (texte effacé puis redessiné en place), puis 5 de ces cartes (As, Deux, Trois, Quatre, Cinq d'Épées) ont reçu une toute nouvelle illustration fournie par l'utilisatrice, déjà correctement au singulier — remplacement direct des fichiers, sans aucun changement de nom ni de code. 5 illustrations (`learn-*.jpg`, 500×500) pour les cases de l'onglet Apprendre — `learn-symboles.jpg`, fournie par l'utilisatrice, complète la « Bibliothèque symbolique » qui n'en avait aucune jusque-là (retour direct : « mets cette illustration, au même format que les autres ») — et 4 illustrations (`suit-*.jpg`) pour les 4 enseignes dans les sous-sections « Figures de cour » et « Cartes numérales ». `reves-hero.jpg` (700×1050) illustre l'onglet Rêves (voir « Rêves — comment ça marche »).
- `assets/logo-mark.png` — logo de marque (clé + soleil + étoile), extrait et recoloré à partir du dos de carte fourni par l'utilisatrice ; affiché en permanence dans le coin haut-gauche de la topbar, sur chaque écran (voir « Filigrane de marque » plus bas)
- `manifest.json` + `icon.svg`/`icon-192.png`/`icon-512.png`/`icon-180.png` — icône d'installation PWA (carré aux bords arrondis avec le logo de marque, voir « Icône d'application » plus bas)
- `service-worker.js` — cache offline
- `politique-confidentialite.html` — page statique et autonome (pas de dépendance à `app.js`/`styles.css`, pour rester consultable même si le reste de l'appli change) requise avant toute publication sur l'App Store/Play Store — voir « Politique de confidentialité » plus bas
- `api/reading.js` — backend serverless (Vercel) qui appelle l'API Anthropic côté serveur
- `api/astral.js` — backend serverless (Vercel) qui calcule un profil astral (thème natal) à partir d'une date/heure/lieu de naissance
- `api/transits.js` — backend serverless (Vercel) qui calcule les positions planétaires du jour, pour l'Horoscope du jour (aucune donnée personnelle reçue)
- `api/portrait.js` — backend serverless (Vercel) qui rédige, une seule fois par profil, un portrait de personnalité (texte suivi) à partir d'un résumé du thème natal
- `api/astral-text.js` — backend serverless (Vercel) qui rédige, une seule fois par profil, une phrase personnalisée pour chaque case du Profil astral (nombre du prénom, chaque planète, l'ascendant, chaque aspect) et explique le choix de la divinité tutélaire
- `api/retrospective.js` — backend serverless (Vercel) qui rédige, une fois par an à l'anniversaire, un bilan de l'année de tirages écoulée à partir d'un résumé agrégé du Journal
- `api/ritual.js` — backend serverless (Vercel) qui rédige, une fois par jour, l'Horoscope du jour affiché à l'accueil : une recommandation unique reliant carte du jour + transits (signes + aspect le plus marqué avec le thème natal) + thème natal fixe (Soleil/Lune/ascendant) + nombre personnel du jour
- `api/comparison-text.js` — backend serverless (Vercel) qui rédige, une seule fois par proche, un texte suivi qui tisse la comparaison de thèmes déjà calculée localement (« Mes proches ») en une lecture fluide adaptée au type de relation
- `api/dream.js` — backend serverless (Vercel) qui interprète un rêve raconté en texte libre, à la manière de l'onirocritique grecque antique (voir « Rêves » plus bas)
- `api/_lib/astro.js` — briques astronomiques partagées entre `api/astral.js` et `api/transits.js` (longitudes écliptiques, signes, aspects) ; le préfixe `_` évite que Vercel n'en fasse une route
- `api/create-checkout-session.js` / `api/checkout-session-status.js` / `api/subscription-status.js` — backend serverless (Vercel) du paiement premium (Stripe), préparé mais pas encore activé côté client — voir « Paiement premium » plus bas
- `package.json` — dépendances `@anthropic-ai/sdk` (`api/reading.js`, `api/portrait.js`, `api/astral-text.js`, `api/retrospective.js`, `api/ritual.js`, `api/comparison-text.js`, `api/dream.js`), `luxon` + `astronomy-engine` (`api/astral.js`, `api/transits.js`), `stripe` (les 3 endpoints de paiement ci-dessus)

## Lecture générée par IA — comment ça marche
La fonction `generateAIReading(question, cards, positions)` dans `app.js` envoie `{question, cards, positions, profile?, history?, memory?}` en `POST` vers `/api/reading` — `positions` (les intitulés des positions du tirage choisi, ex. « Défi », « Résultat ») donne à l'IA le sens de chaque carte dans les tirages autres que le tirage général ; `profile`, `history` et `memory` sont trois résumés optionnels de personnalisation (voir « Profil astral », « Journal intelligent » et « Mémoire du Journal » plus bas). Quand un prénom est enregistré, l'IA est aussi invitée à s'adresser à la personne par son prénom une fois dans la lecture, jamais de façon systématique. Cette fonction serverless (`api/reading.js`), agnostique du type de tirage (1 à 12 cartes, voir `SPREADS` dans `app.js`) :
1. reconstruit le prompt exact (tarologue professionnel, format JSON strict, tient compte des positions si fournies),
2. appelle l'API Anthropic avec la clé stockée dans la variable d'environnement **`ANTHROPIC_API_KEY`** (jamais exposée au navigateur),
3. renvoie `{cards: [...], synthesis}` au client (un texte par carte, dans le même ordre, quel que soit le nombre de cartes du tirage).

Si l'appel échoue ou dépasse son délai (20 à 45 secondes selon le nombre de cartes du tirage — `generateAIReading()` dans `app.js`, le même principe que `max_tokens` côté serveur : plus il y a de cartes, plus l'IA a de texte à rédiger, plus il lui faut de temps), l'app bascule automatiquement sur un texte généré localement (`synthesisParagraphs`/`synthesisParagraphsGeneric` selon le tirage, `interpretationFor`) — l'utilisateur n'est jamais bloqué. Ce repli reste silencieux à l'écran (juste une petite note), mais la cause exacte (délai dépassé, erreur HTTP, réponse invalide) est journalisée dans la console du navigateur pour le diagnostic.

### Protéger l'usage IA (recommandé tant que l'appli est personnelle)
Coût réel : de l'ordre du centime par tirage, variable selon le nombre de cartes (facturation à l'usage sur console.anthropic.com, indépendante d'un éventuel abonnement Claude.ai — les deux sont des produits distincts ; les grands tirages comme la Croix celtique coûtent proportionnellement plus qu'un tirage à 1 ou 3 cartes). Le portrait de personnalité (`/api/portrait`), les textes astraux personnalisés (`/api/astral-text`) et la rétrospective annuelle (`/api/retrospective`) ajoutent chacun un appel comparable, mais respectivement une seule fois par profil, une seule fois par profil et une seule fois par an (mis en cache ensuite). Le texte enrichi de la comparaison de thèmes (`/api/comparison-text`) suit la même logique, une seule fois **par proche** (pas par profil) tant que ses informations de naissance ne changent pas. L'interprétation d'un rêve (`/api/dream`, voir « Rêves » plus bas) n'est, elle, jamais automatique : un appel comparable à un tirage à 1 carte, mais uniquement quand la personne le demande explicitement (bouton « Interpréter ce rêve »), jamais déclenché à l'écriture ni au chargement d'un écran. L'Horoscope du jour (`/api/ritual`) est en revanche **récurrent** : un petit appel supplémentaire chaque jour, du même ordre qu'un tirage à 1 carte. Pour éviter que quelqu'un d'autre qui tomberait sur l'URL de l'appli déclenche des appels avec ta clé :
- Définir une variable d'environnement **`APP_ACCESS_CODE`** (n'importe quelle chaîne secrète). Tant qu'elle est définie, le backend refuse toute requête qui ne fournit pas ce même code — l'appli le demande une fois côté client et le mémorise.
- Recommandé en complément, quel que soit l'usage : fixer un **plafond de dépense mensuel strict** sur console.anthropic.com (Settings → Limits) pour ne jamais pouvoir dépasser un montant que tu choisis.
- Pour ouvrir l'appli à d'autres plus tard : supprimer `APP_ACCESS_CODE` sur Vercel — aucun changement de code nécessaire. Penser alors à revoir le plafond de dépense en conséquence.

## Profil astral — comment ça marche
`POST /api/astral` (`api/astral.js`) calcule un thème astral complet à partir d'une date, heure et lieu de naissance. Reçoit `{ date: "AAAA-MM-JJ", time: "HH:MM" | null, place: string }` (heure **locale** au lieu de naissance ; `time: null` pour une heure de naissance inconnue — voir plus bas) et renvoie :
```json
{
  "resolvedPlace": "Lyon, Rhône-Alpes, France",
  "latitude": 45.75, "longitude": 4.85,
  "timezone": "Europe/Paris",
  "utcInstant": "1990-07-15T12:00:00.000Z",
  "timeUnknown": false,
  "sunSign": "Cancer", "moonSign": "Bélier",
  "ascendant": { "longitude": 201.02, "sign": "Balance", "degreeInSign": 21.02 },
  "midheaven": { "longitude": 115.94, "sign": "Cancer", "degreeInSign": 25.94 },
  "houseSystem": "placidus",
  "houseCusps": [{ "house": 1, "longitude": 201.02 }, "... x12"],
  "houseWarning": null,
  "bodies": {
    "sun": { "longitude": 112.75, "sign": "Cancer", "degreeInSign": 22.75, "house": 9, "retrograde": false },
    "moon": { "...": "..." },
    "mercury": {}, "venus": {}, "mars": {}, "jupiter": {}, "saturn": {}, "uranus": {}, "neptune": {}, "pluto": {}
  },
  "aspects": [{ "bodies": ["sun", "jupiter"], "type": "conjonction", "angle": 0, "orb": 0.2 }, "..."]
}
```
Pipeline en 5 étapes, chacune sur un service/bibliothèque activement maintenu (ou sur des formules maison, pour les étapes 4-5) :
1. **Géocodage** — [Open-Meteo Geocoding API](https://open-meteo.com/en/docs/geocoding-api) (gratuite, sans clé) : convertit le lieu en latitude/longitude, et renvoie directement le **fuseau IANA** du lieu (ex. `"Europe/Paris"`) via GeoNames.
2. **Heure UTC exacte** — [Luxon](https://moment.github.io/luxon/) convertit l'heure locale dans ce fuseau, en tenant compte des règles historiques de changement d'heure (base IANA intégrée à Node). Un contrôle maison détecte les heures locales qui n'ont jamais existé (le "trou" d'un passage à l'heure d'été) et renvoie une erreur claire plutôt que de laisser Luxon deviner silencieusement.
3. **Positions planétaires** — [astronomy-engine](https://github.com/cosinekitty/astronomy) calcule la longitude écliptique géocentrique "de la date" de chaque corps (Soleil à Pluton), d'où sont dérivés signe zodiacal, degré dans le signe et rétrogradation.
4. **Maisons (Placidus) + Ascendant/MC** — calculés à la main par-dessus l'obliquité de l'écliptique et le temps sidéral local que fournit déjà astronomy-engine (pas de dépendance supplémentaire). Algorithme porté d'une implémentation vérifiée contre `swe_houses` (Swiss Ephemeris) à ~1.5' près par la communauté d'astronomy-engine ([discussion #391](https://github.com/cosinekitty/astronomy/discussions/391)). Le système Placidus devient peu fiable au-delà d'environ 66,5° de latitude (cercles polaires) : `houseWarning` le signale dans la réponse plutôt que de renvoyer un résultat silencieusement dégradé.
5. **Aspects** — conjonction, sextile, carré, trigone, opposition entre chaque paire de corps, avec un orbe de 6 à 8° selon le type.

**Heure de naissance inconnue** : envoyer `time: null` fait calculer le thème à midi local (convention standard) ; `timeUnknown: true` et `ascendant`/`midheaven`/`houseCusps`/`houseWarning`/`bodies.*.house` valent `null` dans la réponse (une heure précise est indispensable pour ces calculs-là), mais signes et aspects restent fiables et sont toujours renvoyés.

**Vie privée** : la date/heure/lieu de naissance ne sont utilisées que le temps du calcul (fonction serverless sans état, rien n'est journalisé ni persisté côté serveur) — c'est au client de les garder en `localStorage` s'il veut les réutiliser. Même protection optionnelle `APP_ACCESS_CODE` que `/api/reading` (en-tête `X-App-Access-Code`).

### Client : onglet Profil / Astro
Barre du bas → **Astro** (libellé affiché ; la route interne, `profil()`, `data-route="profil"`, reste inchangée — retour direct d'utilisatrice, un simple renommage superficiel du bouton visible et du titre en haut d'écran, voir `render()`) → une tuile par section : **Profil astral**, **Mes proches** (voir plus bas), et **Mythologie personnelle** (voir « Mythologie personnelle » plus bas, n'apparaît que si un thème astral est enregistré). Première visite de « Profil astral » : formulaire (prénom, **genre**, date/heure/lieu de naissance, case « heure inconnue ») → appel à `/api/astral` → tout le résultat mis en cache dans `localStorage` (`delphesProfile`, jamais renvoyé au serveur ensuite) ; les visites suivantes affichent directement le résultat. Le bouton « Modifier mes informations » (ou « Renseigner mes informations » tant qu'aucun profil n'existe) vit directement dans cet onglet lui-même (`profil()`, sous la grille de tuiles, `data-profil-edit`) plutôt qu'au fond de l'écran « Profil astral » (`renderProfilResults()`) — retour direct d'utilisatrice, ce bouton était auparavant enterré dans un sous-écran qu'il fallait d'abord ouvrir. Comme ce déplacement en fait le seul point d'entrée du formulaire quand un profil existe déjà, le bouton « Annuler » du formulaire (`bindProfilForm()`) ramène désormais vers cet onglet (simple `render()`, `route` restant `"profil"` pendant toute cette navigation) plutôt que vers l'écran « Profil astral ».

**Réorganisation demandée** ("Dans profil, enlève « statistique », « rétrospective » et « journal » et bascule tout ça dans tirage. Renomme « profil » (menu en bas) en « astro ».") : **Journal**, **Statistiques** et la **Rétrospective** annuelle ont quitté cet onglet pour l'onglet **Tirage**, sous une nouvelle section « Ton historique » (voir plus bas) — ils portent sur l'historique des TIRAGES, pas sur le thème astral, et n'avaient donc pas vraiment leur place ici ; Profil/Astro se recentre sur le thème natal, les proches et les arcanes qui s'y rattachent. Le partage d'un même dispatcher de clic entre les deux onglets (`data-screen-go`, ex-`data-profil-go` — renommé pour ne plus sous-entendre "Profil" alors qu'il sert désormais aussi Tirage) évite de dupliquer la logique de navigation. Dans le même mouvement, le tirage **« Année à venir »** (une carte par mois restant de l'année) est retiré de l'onglet Tirage — `SPREADS.annee` et tout ce qui n'existait que pour lui (`MONTH_NAMES`, `remainingMonthsPositions()`, `YEAR_DEFAULT_QUESTION`) ont disparu avec lui ; il reste 4 types de tirage (voir plus bas).

**Genre** (champ optionnel du formulaire, « Féminin » / « Masculin » / « Préfère ne pas préciser » — ce dernier par défaut) : sert uniquement à accorder correctement les textes rédigés par l'IA (portrait, textes astraux, comparaison de thèmes), jamais à autre chose. Retour direct d'utilisatrice ("il faut penser à mettre le sexe... pour que le texte soit généré avec le bon genre"). Transmis tel quel dans `profileForPortrait()`/`profileForAstralText()` (`gender: "f" | "m" | null`) et dans `profileForComparisonText()` (`yourGender`/`theirGender`, chacun indépendant). Côté serveur, chaque endpoint IA accorde explicitement le texte au genre connu ; quand il n'est pas précisé, le prompt demande explicitement une formulation neutre (déjà le comportement par défaut avant l'ajout de ce champ — aucune régression pour un profil existant qui ne le renseigne pas). Dans `api/comparison-text.js`, `genderNote()` gère les deux personnes indépendamment : le genre de l'une peut être connu sans que celui de l'autre le soit — la règle "jamais de pronom genré" ne s'assouplit que pour la personne dont le genre est effectivement connu. Le même champ existe sur chaque proche (« Mes proches », voir plus bas) ; `comparisonFingerprint()` inclut désormais aussi le genre des deux thèmes, pour que le texte de comparaison en cache se régénère automatiquement si l'un des deux genres est renseigné ou modifié après coup.

**Contexte de vie** (4 champs optionnels du formulaire, tous "Préfère ne pas préciser" par défaut) : **Métier** (texte libre), **Situation amoureuse** (Célibataire / En couple / Marié·e ou pacsé·e / Divorcé·e ou séparé·e / Veuf·ve), **As-tu des enfants ?** (Oui/Non) et **Centres d'intérêt** (texte libre). Retour direct d'utilisatrice ("le métier a une grande importance pour l'interprétation des rêves, et pour les tirages tarot aussi"). `lifeContextFor(saved)` dans `app.js` construit un résumé unique à partir de ces 4 champs (`LOVE_STATUS_LABELS` traduit le code stocké en libellé humain, `hasChildren` devient `true`/`false`/`null`) — renvoie `null` si absolument rien n'est renseigné, pour ne rien ajouter au prompt dans ce cas. Réutilisé par 3 endpoints : `profileForReading()`/`profileForPortrait()` l'incluent dans leur résumé habituel (champ `lifeContext`) ; `profileForDream()` l'envoie seul (sans le thème astral) à `/api/dream`, un rêve restant avant tout un texte à interpréter pour lui-même. Chaque endpoint tisse ce contexte avec subtilité si c'est pertinent, jamais listé à part ni au détriment des cartes tirées/du récit du rêve. Uniquement sur le profil principal pour l'instant, pas encore sur les proches (« Mes proches »).

Le résultat n'est pas qu'une liste brute de signes et degrés : `natalSummaryParagraph()` ouvre l'écran par un paragraphe récapitulatif (Soleil/Lune/Ascendant + densité des aspects), et chaque planète, l'Ascendant et chaque aspect ont 1-2 phrases d'explication (`natalPlanetSentence()`/`natalAscendantSentence()`/`natalAspectSentence()`) — composées à partir d'un vocabulaire réutilisable (`PLANET_THEMES` : le domaine de vie de chaque planète ; `signQuality()` : le mot-clé du signe, via `ZODIAC_MAJOR_LINKS`, la même table que pour « Apprendre »). Entièrement local, aucun appel IA — c'est le texte affiché tant que `/api/astral-text` (voir plus bas) n'a pas encore répondu, ou en repli s'il échoue.

Le prénom reçoit en plus une **numérologie** (`nameNumerology()` dans `app.js`, méthode pythagoricienne classique réduite à 1-9, volontairement sans nombre maître 11/22/33) dont la signification réutilise directement `NUMBER_KEYS`, la même grammaire symbolique que les cartes numérales.

Contrairement au nombre du prénom (fixe à vie), trois nombres évoluent avec le temps — la **numérologie du temps** (`personalYearNumber()`/`personalMonthNumber()`/`personalDayNumber()`, formule classique jour+mois de naissance+année en cours [+mois en cours [+jour en cours]], même réduction 1-9) : l'année personnelle change chaque 1er janvier, le mois personnel chaque mois, le jour personnel chaque jour. Les deux premiers sont affichés dans l'écran Profil astral juste sous le nombre du prénom ; le jour personnel, lui, alimente uniquement l'Horoscope du jour (voir plus bas), où sa granularité quotidienne compte davantage qu'un affichage statique.

Quand un profil est enregistré, `generateAIReading()` envoie automatiquement un résumé (`profileForReading()` : prénom, nombre numérologique, signes solaire/lunaire/ascendant, année personnelle) à `/api/reading`, qui l'intègre avec subtilité à la lecture si c'est pertinent — sans jamais devenir un horoscope générique ni éclipser les cartes tirées. Sans profil enregistré, la lecture se comporte exactement comme avant.

**Divinité tutélaire** : `tutelaryDeity()` détermine la figure mythologique la plus représentée dans le thème natal — chaque planète « vote », avec un poids plus fort pour les planètes personnelles (Soleil, Lune, Ascendant, Mercure, Vénus, Mars) que pour Jupiter/Saturne, et en excluant Uranus/Neptune/Pluton — trop lentes pour différencier un individu d'une génération entière. Le vote se fait à la précision maximale que l'illustration du jeu permet à ce jour : d'abord le **décan exact** du signe (tranche de 10°, `DECAN_MINOR_CARDS`/`decanCardFor()` — tradition des "decanates" du Golden Dawn/Thoth Tarot, chaque signe divisé en 3 décans pointant chacun vers une carte numérale 2-10 d'une enseigne selon sa triplicité élémentaire) ; si cette carte a déjà sa propre figure illustrée (`NUMBER_CARD_DEITY` — les 40 cartes numérales des 4 enseignes en ont désormais une), le vote va à cette figure précise, piochée dans l'ensemble de la bibliothèque (~104 figures) plutôt que dans les seules 12 divinités des majeurs ; le repli sur le dieu du majeur associé au signe entier via `ZODIAC_MAJOR_LINKS` (comportement d'avant cette extension) reste dans le code par robustesse, mais n'est plus jamais déclenché en pratique : avec l'illustration du 10 de Deniers (Euthénie), les 36 décans des 12 signes du zodiaque votent tous désormais avec la précision maximale. Affichée sous forme de carte cliquable (glyphe + nom + note), qui renvoie vers la fiche complète de la divinité, avec une explication de ce choix (voir `/api/astral-text` juste en dessous : `tutelaryReason` en IA, ou une phrase de repli qui énumère les placements les plus lourds via `tutelaryReasonFallback()`).

**Portrait de personnalité** (`POST /api/portrait`, `api/portrait.js`) : un texte suivi de 3-4 paragraphes, rédigé par l'IA à partir d'un résumé du thème (signes, numérologie, divinité tutélaire, aspects les plus serrés — jamais le thème natal complet), sans jargon technique ("aspect", "orbe"…) traduit en traits de caractère concrets. Généré **une seule fois** et mis en cache dans le profil (`profile.portrait`), jamais régénéré tant que les informations de naissance ne changent pas — voir `ensurePortrait()` dans `app.js`, même mécanique de discrétion qu'`ensureTransits()` (aucun appel avant qu'un code d'accès existe déjà). Remplace le paragraphe récapitulatif local (`natalSummaryParagraph()`) une fois prêt ; celui-ci reste affiché en attendant ou si l'appel échoue.

**Textes astraux personnalisés** (`POST /api/astral-text`, `api/astral-text.js`) : à la différence du portrait (un texte d'ensemble), une phrase distincte générée par l'IA pour *chaque* case — le nombre du prénom, chaque planète, l'ascendant, chaque aspect (plafonné aux 10 aspects les plus exacts par orbe) — plus l'explication de la divinité tutélaire et celle des arcanes majeurs liés au thème (voir juste en dessous). `profileForAstralText()` construit le résumé envoyé (mêmes principes de sobriété que pour le portrait), avec une clé stable par aspect (`sun_carré_moon`, etc., voir le commentaire dans `app.js`) pour que le texte retrouve la bonne case quel que soit l'ordre. Générées **une seule fois** et mises en cache dans le profil (`profile.astralText`), régénérées si les informations de naissance changent (même mécanique que `ensurePortrait()`) **ou** si la divinité tutélaire recalculée en direct ne correspond plus à celle pour laquelle le cache a été écrit (`astralText.tutelaryDeityKey`, comparé dans `ensureAstralText()`) — ce second cas couvre à la fois les profils enregistrés avant l'extension aux décans ci-dessus et, plus tard, l'illustration de Coupes/Deniers qui affinera le calcul pour d'autres personnes : sans ce garde-fou, `tutelaryReason` continuerait de justifier une figure différente de celle réellement affichée. Tant qu'un champ précis n'est pas encore généré (ou en cas d'échec), `renderProfilResults()` retombe silencieusement sur la phrase locale correspondante (`natalPlanetSentence()` etc.) — jamais de case vide.

**Arcanes majeurs liés au profil** (onglet Profil, tuile dédiée « Arcanes majeurs liés » → écran séparé `renderMajorLinks()`/`showMajorLinks()`, route `"majeurs"` — cette section vivait auparavant directement dans `profil()`, déplacée sur son propre écran pour lui laisser toute la place et garder l'onglet Profil épuré ; la tuile n'apparaît que si un thème astral est enregistré) : Soleil, Lune et Ascendant sont chacun rattachés à un arcane majeur via `ZODIAC_MAJOR_LINKS` — une correspondance signe entier → carte fixe, de tradition ésotérique classique (Golden Dawn), la même table qui sert de repli à `tutelaryDeity()` (voir plus haut) et à la mise en avant de "tes" cartes dans Apprendre. `majorLinksFor()` calcule cette correspondance et regroupe Soleil/Lune/Ascendant qui tomberaient sur la même carte plutôt que de l'afficher en double — logique factorisée pour être réutilisée aussi bien par l'écran (`profileMajorLinks()`) que par le résumé envoyé à l'IA (`profileForAstralText()`). La grille de cartes elle-même (quel signe pointe vers quelle carte) reste une donnée brute, gratuite ; ce qui est réservé au premium, comme le reste de l'interprétation écrite du Profil astral (même bloc `premiumLockHTML()` en mode gratuit), c'est l'explication — pourquoi CES cartes précisément, et ce qu'elles disent de la personne. Cette explication est un texte IA (`majorLinksText`, généré par `/api/astral-text`, mis en cache dans `astralText` comme le reste) qui s'appuie sur le dieu et les mots-clés de chaque carte, déjà connus localement et jamais devinés par l'IA. Retour direct d'utilisatrice après un premier essai jugé "trop court, pas explicite" : le prompt exige désormais **un paragraphe distinct par carte, d'au moins 5-6 phrases chacun** (jamais une phrase expédiée par carte) — pour chaque carte, 2-3 phrases sur le pourquoi du lien, puis **au moins 3 phrases développées et concrètes** sur ce que la carte révèle vraiment de la personne ET ce que ça lui apporte concrètement de s'y reconnaître (un trait profond, une tension, illustrés par une situation de vie plausible, puis ce que cette prise de conscience permet de faire ou de voir différemment), cette seconde partie étant explicitement celle qui doit apporter le plus de valeur. Séparés par un retour à la ligne double comme les autres textes de l'app, éclatés en plusieurs `<p>` par `renderMajorLinks()` (pas un seul bloc).

Deuxième retour, plus précis, sur ce même premier round de correctifs : le "pourquoi" ("le Soleil en Lion te relie à La Force… n'est pas satisfaisante ! Pourquoi ?") restait abstrait — un simple rappel du signe, sans jamais expliquer ce qui relie vraiment le dieu de la carte à ce qu'elle représente. `ZODIAC_MAJOR_MYTH` (nouvelle table, une entrée par signe des 12 de `ZODIAC_MAJOR_LINKS`) fournit désormais, pour chaque lien, deux faits vérifiables : `astro` (le lien traditionnel signe → carte : élément, planète maîtresse, symbolisme) et `myth` (un épisode mythologique grec réel et concret expliquant pourquoi ce dieu incarne cette carte — ex. pour Lion → La Force/Héraclès : le lion de Némée, son premier des douze travaux, terrassé à mains nues). `profileForAstralText()` transmet ces deux champs (`mythAstro`/`mythFact`) à `/api/astral-text`, dont le prompt exige désormais explicitement que l'IA s'appuie dessus (reformulés, jamais inventés ni remplacés par un autre mythe) plutôt que de rester sur une correspondance abstraite. `majorLinksTextFallback()` (la phrase de secours locale, tant que le texte IA n'est pas encore en cache) s'appuie sur la même table, et répond en plus, en termes génériques, à "qu'est-ce que ça apporte" via les mots-clés de la carte — jamais un simple énoncé mécanique, jamais de case vide en attendant l'IA (même principe que `tutelaryReasonFallback()`).

**Correctif d'un vrai bug de cache** : un profil enregistré **avant** l'ajout de `majorLinksText` (ou de tout futur champ du même genre) avait un `astralText.tutelaryDeityKey` déjà à jour (rien n'avait changé côté divinité tutélaire) mais un cache qui, logiquement, ne contenait pas ce nouveau champ — `ensureAstralText()` ne comparait alors QUE `tutelaryDeityKey`, jugeait le cache toujours valable et ne relançait donc jamais d'appel IA : ces profils restaient bloqués indéfiniment sur `majorLinksTextFallback()`, ce qui explique très concrètement le retour ci-dessus. `ensureAstralText()` vérifie désormais en plus que `cached.majorLinksText` existe bel et bien quand des arcanes majeurs sont attendus (`majorLinksFor(p.astral)` non vide) avant de considérer le cache comme suffisant.

**Troisième retour, sur le texte une fois vraiment généré** : la même formule ("pas comme un trait figé une fois pour toutes...") revenait à l'identique sur les 3 points (Soleil/Lune/Ascendant) — moins naturel, moins personnalisé — et surtout Soleil en tel signe était traité comme équivalent à Lune ou Ascendant dans ce même signe, alors que ces trois points n'engagent pas le même domaine de vie. Restructuration en conséquence : `majorLinksFor()` continue de regrouper Soleil/Lune/Ascendant par carte pour la grille visuelle (`renderMajorLinks()`), mais `profileForAstralText()` envoie désormais à l'IA **une entrée par POINT**, jamais groupée — avec un nouveau champ `pointDomain` (voir `POINT_DOMAIN_HINT` : identité/vitalité pour le Soleil, monde intérieur/instinct pour la Lune, image perçue par les autres pour l'Ascendant). Le prompt exige en conséquence **un paragraphe par point** (même quand deux points partagent la même carte — le second y renvoie brièvement plutôt que de réexpliquer le mythe depuis le début), une différenciation explicite du contenu via `pointDomain`, et interdit formellement de réutiliser la même formule de conclusion d'un paragraphe à l'autre. `majorLinksTextFallback()` (la phrase de secours locale) suit la même logique : un paragraphe par point, chacun avec sa propre phrase "qu'est-ce que ça apporte" tirée de `POINT_DOMAIN_HINT`, plutôt qu'une formule générique recopiée trois fois.

**"C'est encore le même texte, c'est normal ?"** : oui, et c'était un vrai bug — après la réécriture ci-dessus, un texte déjà en cache passait toujours les deux vérifications existantes d'`ensureAstralText()` (divinité tutélaire inchangée, `majorLinksText` déjà présent) et ne se régénérait donc jamais tout seul, même si le PROMPT lui-même avait changé en profondeur entre-temps. `MAJOR_LINKS_TEXT_VERSION` (nouvelle constante) sert de numéro de version explicite pour ce texte précis : `ensureAstralText()` compare désormais aussi `cached.majorLinksTextVersion` à cette constante, et régénère si elles ne correspondent pas — à incrémenter à chaque futur changement du prompt qui change substantiellement le texte produit, indépendamment de tout autre invalidateur de cache.

**"Le texte n'a toujours pas changé"** (malgré le correctif ci-dessus, déjà bien déployé côté serveur — vérifié via les en-têtes HTTP `cache-control: public, max-age=0, must-revalidate` sur `app.js`/`service-worker.js`, donc pas de cache CDN figé) : la cause restante était plus profonde, et touche n'importe quel futur correctif, pas seulement celui-ci. Une PWA déjà ouverte (ou installée sur l'écran d'accueil, où elle n'est presque jamais "rechargée" par l'utilisatrice) continue d'exécuter le JavaScript déjà chargé en mémoire indéfiniment — un nouveau `service-worker.js` peut très bien s'installer et prendre le contrôle en arrière-plan (`self.skipWaiting()`/`self.clients.claim()`) sans que la page déjà ouverte s'en rende compte, puisque prendre le contrôle des futures requêtes réseau ne change rien au code déjà interprété par le moteur JS. Il n'existait jusqu'ici **aucun mécanisme de rechargement automatique** : `app.js` se contentait d'appeler `navigator.serviceWorker.register(...)`, sans jamais réagir à une mise à jour. Corrigé en écoutant l'évènement `controllerchange` (déclenché précisément quand un nouveau service worker prend le relais, y compris dès la toute première visite grâce à `skipWaiting`/`clients.claim`) et en rechargeant la page une seule fois à ce moment-là (`refreshing`, un simple drapeau, évite toute boucle si l'évènement se déclenchait plusieurs fois). Sans danger pour un brouillon de tirage ou de rêve en cours : chaque frappe est déjà sauvegardée en continu dans `localStorage` (`tirageState`/`dreamState`), donc rien n'est perdu au rechargement. Concrètement, ce correctif rend tous les futurs déploiements auto-appliqués à la prochaine ouverture de l'app, sans plus jamais nécessiter d'explication du type "ferme et rouvre l'appli pour voir le changement".

**"Ça marche toujours pas"** : le correctif ci-dessus (`controllerchange`) était nécessaire mais pas suffisant — il ne pouvait aider qu'à partir du moment où il tournait déjà lui-même, or l'ouverture qui aurait dû le déclencher exécutait forcément encore l'ancien `app.js` (celui d'avant ce correctif), incapable de s'auto-réparer. La cause plus profonde, elle, touchait la stratégie même du cache dans `service-worker.js` : **cache d'abord, réseau en repli** (`caches.match(event.request).then(cached=>cached||fetch(...))`). Concrètement, une PWA rouverte après un déploiement était systématiquement servie par LE SERVICE WORKER ENCORE ACTIF au moment précis de cette requête — potentiellement une version d'avant le tout dernier déploiement — tant que le nouveau n'avait pas fini de s'installer puis de s'activer en arrière-plan (`skipWaiting()`/`clients.claim()` prennent toujours un instant, jamais zéro) ; un premier rechargement après un déploiement retombait donc quasi systématiquement sur l'ancien code, peu importe ce que contenait déjà `app.js` à cet instant. Stratégie inversée : **réseau d'abord, cache seulement en secours hors-ligne** — chaque requête GET tente d'abord le réseau (et met à jour le cache avec la réponse obtenue), ne retombant sur le cache que si le réseau échoue vraiment (hors-ligne). Comme les fonctionnalités IA nécessitent de toute façon une connexion, l'appli est réellement hors-ligne rarement — dans l'immense majorité des cas, chaque chargement récupère donc directement la dernière version depuis le serveur, sans plus dépendre du cycle de vie ni du timing du service worker. Les requêtes POST (tous les appels IA vers `/api/*`) restent, comme avant, jamais interceptées par le cache. `CACHE` bumpé à `delphes-v87` pour ce changement.

**"Le texte a changé [...] mais les trois textes terminent pareil [...] la relation avec l'arcane choisie n'est pas vraiment mise en valeur, tu parles plutôt du signe astrologique"** : cette fois, le vrai coupable était bien de retour dans le code lui-même — précisément dans `majorLinksTextFallback()` (la phrase de secours locale). La clôture de paragraphe introduite au round précédent ne variait que par substitution de mots (le nom du point) dans un gabarit par ailleurs identique, avec la même clause finale mot pour mot ("se joue avant tout à cet endroit-là, pas comme un trait unique...") sur les 3 paragraphes — et cette clôture parlait du "point" et de son "domaine" de façon abstraite plutôt que de la carte elle-même, qui n'était citée qu'en ouverture de paragraphe. Double correctif :
- `POINT_CLOSING_SENTENCE` (nouvelle table, une fonction par point plutôt qu'un gabarit à trous) donne à chaque point sa propre PHRASE entière — pas seulement ses propres mots — et chacune démarre par « Cette carte parle alors de... » : la carte reste le sujet grammatical de la clôture, le point n'étant que l'angle (ce que tu es en plein jour pour le Soleil, ce que tu vis en privé pour la Lune, l'image que tu donnes pour l'Ascendant) par lequel elle s'exprime.
- Le prompt de `/api/astral-text.js` (qui n'avait pas ce problème de répétition littérale, déjà interdite explicitement, mais pouvait dériver vers un paragraphe surtout astrologique) précise désormais que le "Domaine de vie propre à ce point" ne sert qu'à choisir l'angle, jamais à devenir lui-même le sujet du paragraphe : la carte (son nom, son dieu, ses mots-clés) doit rester le fil conducteur de la plupart des phrases. La formule de repli "se joue avant tout à cet endroit-là" a été ajoutée à la liste des tournures interdites, en plus de l'ancienne.
- `MAJOR_LINKS_TEXT_VERSION` : 2 → 3, pour que tout texte déjà en cache avec l'ancien prompt soit régénéré avec ce nouveau. `max_tokens` de `/api/astral-text.js` : 3600 → 4200 (marge de sécurité par précaution, au cas où une réponse complète — nombre du prénom + toutes les planètes + tous les aspects + tutelaryReason + jusqu'à 3 paragraphes de 5-6 phrases — approcherait la limite précédente sur un thème très rempli).

**"Il faut mettre les illustrations des cartes avant chaque texte"** : `renderMajorLinks()` affichait jusque-là tout le texte d'un bloc en haut de l'écran, puis une grille de cartes séparée tout en bas — l'illustration d'une carte n'était donc jamais visuellement associée à son propre paragraphe. Restructuré pour entrelacer les deux : `majorLinksText` (un paragraphe par POINT, voir plus haut) est découpé, puis chaque lien de `links` (un lien par CARTE, éventuellement partagée par plusieurs points) reprend `l.labels.length` paragraphes consécutifs — exactement ceux qui la concernent, puisqu'ils sont générés dans le même ordre (par lien, puis par point au sein de ce lien) aussi bien par `majorLinksTextFallback()` que par le prompt IA. Résultat : illustration, puis texte, pour chaque carte l'une après l'autre — et pour une carte partagée entre deux points (ex. Soleil et Ascendant sur la même carte), l'illustration n'apparaît qu'une seule fois, suivie de ses deux paragraphes. La grille de cartes séparée en bas d'écran disparaît complètement.

**"Les textes ne correspondent pas aux cartes"** : le découpage positionnel ci-dessus (`majorLinksText.split(...)` puis `l.labels.length` paragraphes consécutifs) supposait que l'IA renvoie toujours exactement un paragraphe par point, dans le même ordre et avec le même séparateur que côté client — une seule réponse un peu différente (un paragraphe fusionné, un de plus ou de moins, un séparateur légèrement différent) suffisait à décaler silencieusement tout ce qui suit, sans aucune erreur visible : le texte d'un point se retrouvait affiché sous la mauvaise carte. Correctif structurel plutôt qu'un simple réglage du prompt : `majorLinksText` n'est plus une chaîne à découper mais un **tableau d'objets `{point, text}`**, un par point, chacun explicitement étiqueté par le nom exact du point qu'il concerne. Le prompt de `/api/astral-text.js` exige ce format tableau et interdit tout texte fusionné ou point sauté ; la réponse est ensuite filtrée côté serveur contre l'ensemble réel des points demandés (`validPoints`), donc une étiquette inventée ou mal orthographiée par l'IA est simplement écartée plutôt que mal associée. Côté client, `renderMajorLinks()` va chercher le texte de chaque point par son nom (`textForPoint()`, `.find(e=>e.point===label)`) au lieu d'une position dans un tableau découpé — plus aucune dépendance à l'ordre ou au nombre de paragraphes renvoyés. `majorLinksTextFallback()` (repli local) suit le même format. `MAJOR_LINKS_TEXT_VERSION` : 3 → 4, pour régénérer tout texte déjà en cache sous l'ancien format string.

**"La tuile « arcanes liés » n'est plus cliquable !"** : effet de bord du correctif ci-dessus, pour tout profil dont le cache local avait été écrit AVANT lui. `ensureAstralText()` détecte bien le changement de format (`MAJOR_LINKS_TEXT_VERSION` a changé) et relance un appel IA — mais cet appel est asynchrone, alors que `renderMajorLinks()` lit le cache existant tout de suite après, de façon synchrone : le temps que la réponse arrive, `getCachedAstralText().majorLinksText` reste encore l'ANCIEN format (une simple chaîne). `aiMajorLinksText.find(...)` plantait donc sur cette chaîne, ce qui faisait échouer tout l'écran — et comme l'exception se produisait dans le clic sur la tuile "Arcanes majeurs liés" elle-même (`showMajorLinks()` → `renderMajorLinks()`), l'écran ne changeait jamais : vu de l'extérieur, la tuile semblait avoir cessé de réagir. Corrigé en ne faisant confiance au texte IA en cache que s'il est bien un tableau (`Array.isArray(...)`) — sinon `textForPoint()` retombe sur `majorLinksTextFallback()` pour ce point, le temps que le nouvel appel IA se termine, exactement comme le ferait un profil sans aucun cache.

**"La divinité tutélaire est passée à Techné [...] mais il n'y a pas l'illustration !"** : `deity.card[2]` n'a jamais été qu'un simple glyphe emoji (le même champ que celui affiché, par exemple, en tête de chaque fiche d'arcane majeur) — la vraie illustration peinte de la carte (`CARD_IMAGES`, déjà utilisée partout ailleurs via `cardHTML()`) n'était tout simplement jamais appelée dans le bloc "Ta divinité tutélaire" de `renderProfilResults()`, quelle que soit la divinité obtenue. Corrigé en insérant `cardHTML(deity.card, ...)` juste après le titre de la section, avant le nom et la note de la divinité — et en appelant `bindCards()` en plus de `bindChips()` dans `showProfilAstral()` pour que cette illustration ouvre bien la fiche de la carte au clic (le nom de la divinité, lui, continue d'ouvrir sa propre fiche mythologique). Retour immédiat sur ce premier essai : mal centrée. En cause, le `card-grid` (`repeat(auto-fill,minmax(150px,1fr))`) dans lequel elle était insérée, pensé pour plusieurs cartes côte à côte — avec une seule carte, elle se retrouvait collée à gauche de la première colonne plutôt que centrée. Retiré : `cardHTML()` est appelée directement dans `.detail`, exactement comme `showDetail()` pour la fiche d'une carte seule, où `.detail .tarot-card{max-width:270px;margin:10px auto 20px}` s'occupe déjà de la centrer et de la tailler proprement.

**« Mythologie personnelle »** (consolidation demandée après coup — "Très bon point regroupe tout au même endroit sous « mythologie personnelle » c'est cet onglet qui me permettra de générer les bracelets personnalisés") : divinité tutélaire, animal représentatif et arcanes majeurs liés répondent tous les trois à la même question — « qu'est-ce que mon thème dit de moi mythologiquement ? » — mais vivaient jusque-là à deux endroits séparés (les deux premières dans l'écran « Profil astral », la 3e sur son propre écran « Arcanes majeurs liés »). Regroupées sur un seul écran, `renderPersonalMythology()`/`showPersonalMythology()` (route interne toujours `"profil"`, ex-`renderMajorLinks()`/`showMajorLinks()` — renommées avec ce qu'elles affichent désormais), dans cet ordre : divinité tutélaire, animal représentatif, puis arcanes majeurs liés. La tuile « Arcanes majeurs liés » de l'onglet Profil/Astro devient « Mythologie personnelle » (`data-screen-go="mythologie"` — l'attribut, déjà partagé avec Tirage depuis la réorganisation précédente, reste `data-screen-go` plutôt que redevenir `data-profil-go`), affichée dès qu'un thème astral est enregistré (`saved.astral`) — la divinité et l'animal ne dépendaient auparavant que de la présence d'un profil (via l'écran Profil astral), pas spécifiquement d'un lien vers un arcane majeur (`profileMajorLinks()`), mais en pratique les deux conditions coïncident presque toujours (un thème astral complet fournit toujours au moins un signe). L'écran « Profil astral » (`renderProfilResults()`) perd les deux sections « Ta divinité tutélaire » et « Ton animal représentatif » — il ne montre plus que le portrait, le résumé et les données chiffrées du thème (numérologie, planètes, aspects) ; `bindChips()`/`bindCards()`, qui ne servaient plus qu'à ces deux sections dans `showProfilAstral()`, sont retirés (plus rien à y rendre cliquable). Explicitement pensé comme point d'ancrage pour une future fonctionnalité de bracelets personnalisés (un dieu, un animal, des arcanes — la même matière symbolique qui composera leurs breloques, voir « Pistes de suite possibles » plus bas).

**Animal représentatif (fonctionnalité entièrement retirée depuis — voir plus bas pour l'historique complet)** : `representativeAnimal()` déterminait un animal symbolique (catégorie "Animaux" de `SYMBOL_LIBRARY`) à partir du signe solaire du thème natal (`SIGN_ANIMAL`, un animal par signe) et, dans une première version, d'un second signal — le domaine de question le plus fréquent du Journal (`DOMAIN_ANIMAL`). Se débloquait avec le mode premium, un simple interrupteur local (`isPremiumEnabled()`/`setPremiumEnabled()`, clé `delphesPremium` dans `localStorage`). Explication toujours locale/déterministe, sans appel IA.

**"Je préfère que chaque signe tombe sur son « vrai » animal [...] et les signes sans animal, là tu fais une association"** : `SIGN_ANIMAL` liait jusque-là chaque signe à un animal choisi pour un lien mythologique parfois assez lâche (un "écho thématique" plutôt qu'un vrai animal du mythe fondateur). Reconstruit signe par signe : six signes SONT littéralement un animal dans le mythe qui a donné son nom à leur constellation — Bélier (le bélier à la Toison d'or, nouvelle fiche `SYMBOL_LIBRARY["bélier"]`), Taureau (déjà l'animal lui-même), Cancer (le crabe envoyé par Héra contre Héraclès, nouvelle fiche `"crabe"`), Lion (le lion de Némée — fiche déjà existante mais classée par erreur "Mythologie" plutôt que "Animaux", recatégorisée), Scorpion (envoyé par Apollon contre Orion, nouvelle fiche `"scorpion"`, cohérente avec le récit déjà écrit dans la fiche « Orion »), Poissons (Aphrodite et Éros changés en poissons pour fuir Typhon, nouvelle fiche `"poisson"`). Deux signes sont des hybrides mi-homme/mi-animal dans leur mythe : Sagittaire (le centaure, déjà `"cheval"`, inchangé) et Capricorne (la chèvre-poisson Amalthée, nouvelle fiche `"chèvre"`, qui renvoie à la fiche déjà existante « Corne d'abondance » pour le reste du mythe). Les quatre signes restants n'ont pas d'animal propre dans leur mythe (des figures humaines ou divines : les Dioscures, une vierge, une balance, un échanson) — une association reste nécessaire, mais choisie pour un lien mythologique direct plutôt qu'un simple écho : Gémeaux passe de `"papillon"` à `"cygne"` (Castor et Pollux naissent de l'union de Zeus-cygne et Léda) et Verseau passe de `"chien"` à `"aigle"` (l'aigle de Zeus qui enlève Ganymède, lui-même devenu le Verseau — un paragraphe est ajouté à la fiche « Aigle » existante pour l'expliciter) ; Vierge (`"abeille"`, Déméter/Mélissai) et Balance (`"colombe"`, Vénus/Aphrodite régissant la Balance en astrologie) restent inchangés, déjà correctement sourcés. `SYMBOL_LIBRARY` passe ainsi de 85 à 90 entrées (5 nouvelles fiches complètes, chacune avec son propre `lore`).

**"Pour simplifier on va enlever le domaine par question dominant du journal"** : `DOMAIN_ANIMAL` et le seuil `REPRESENTATIVE_ANIMAL_MIN_READINGS` (10 tirages, qui n'existait que pour laisser ce second signal se dégager d'un vrai motif plutôt que d'une coïncidence) sont retirés — l'animal ne dépend plus que du signe solaire, débloqué dès que le mode premium est actif, exactement comme la divinité tutélaire, sans plus aucune dépendance au Journal. `journalTrends().topDomain` continue d'exister et d'alimenter la bannière « Tes tendances » du Journal ainsi que les Statistiques (`domainDistribution`) — seul son usage dans `representativeAnimal()` disparaît.

**"En fait on va carrément l'enlever. [...] Garde bien les textes écrit sur les animaux"** : l'Animal représentatif disparaît cette fois entièrement (`SIGN_ANIMAL`, `representativeAnimal()`, et la section "Ton animal représentatif" de `renderPersonalMythology()`) — « Mythologie personnelle » ne garde plus que la divinité tutélaire et les arcanes majeurs liés, jusqu'à 3 illustrations au total selon Soleil/Lune/Ascendant (voir « Mythologie personnelle » plus haut), pensées comme base d'un futur bracelet personnalisé. Consigne explicite respectée : les 6 fiches mythologiques écrites pour cette fonctionnalité (`bélier`, `crabe`, `lion` — recatégorisé depuis "Mythologie" plutôt que créé —, `scorpion`, `chèvre`, `poisson`, plus le paragraphe Ganymède/Verseau ajouté à `aigle`) restent intactes dans `SYMBOL_LIBRARY`, normalement consultables dans Apprendre → Symboles — seule la table de correspondance signe → animal et la fonction qui la consommait ont disparu.

**Filigrane de marque, navigation Précédente/Suivante dans Apprendre, et bouton retour contextuel** (demande groupée, à partir d'une photo du dos de carte fourni : "Garde juste la clé (clé + soleil + étoile. Enlève le fond et les dessins autour). Rends la doré (garde juste le terracotta à l'intérieur). Mets la sur chaque page, de manière discrète mais présente. Quand on consulte les carte (dans l'onglet apprendre) j'aimerai que lon puisse passer à la carte d'après ou d'avant sans avoir à cliquer sur retour et retourner au menu. De plus, chaque petit bouton retour doit ramener à la page consultée précédemment (et ce dans chaque onglet) et non pas sur l'accueil") : trois correctifs indépendants livrés ensemble.
- **Le logo** : extrait de la photo du dos de carte par seuillage de luminance (Python/Pillow, pas d'outil génératif — un simple masque `luminance > seuil` sépare déjà nettement l'emblème doré du fond terracotta sombre), recadré sur le seul sunburst + anneau + clé (bordure, coins à motifs et étoiles décoratives du dos de carte écartés). Recoloré en aplat `var(--gold)` partout, sauf le disque intérieur et l'étoile boussole à 8 branches au cœur du fer de clé, qui restent en `var(--terracotta)` (repérés par une seconde règle de classification sur la teinte d'origine — plus sombres et moins jaunes que l'or environnant — plutôt qu'une zone dessinée à la main). `assets/logo-mark.png`, PNG transparent, 240px de large. Affiché en `<img class="brand-mark">` dans `index.html`, **hors de `#screen`** — comme `#soundBtn` juste au-dessus, ça le rend persistant à travers tous les re-rendus de `render()`, donc visible sur les 5 onglets et tous leurs sous-écrans sans qu'aucun écran n'ait besoin de le connaître. Discret par construction (`opacity:.4`, `pointer-events:none`, 30px de large à l'écran, coin bas-gauche — symétrique à `#soundBtn` côté droit) : un repère silencieux, jamais un bouton de plus.
- **Précédente/Suivante** sur la fiche d'une carte (`showDetail()`), mais seulement quand elle a été ouverte depuis une grille ORDONNÉE de l'onglet Apprendre — tous les Majeurs (`showLearnMajors()`) ou les cartes d'une même enseigne (`showLearnSuit()`). `bindCards(list)` accepte désormais cette liste et la transmet à `showDetail(carte, backTo, list)` ; `showDetail()` y retrouve l'index de la carte courante (`c[0]`, unique dans tout le jeu) pour calculer la précédente/suivante et affiche deux boutons (`.detail-card-nav`, désactivés en bout de liste) qui rouvrent la fiche voisine sans jamais repasser par la grille — `backTo` et `list` restent inchangés d'une carte à l'autre, donc « ← Retour » ramène toujours à la bonne grille de départ quel que soit le nombre de cartes parcourues entre-temps. Tous les autres appels de `bindCards()` dans l'app (résultats de tirage, « cartes concernées » d'un symbole/figure/nombre…) n'ont toujours pas de liste : ces boutons n'y apparaissent pas, comportement inchangé.
- **Bouton retour de la topbar** (`#backBtn`, le chevron `‹` à côté du titre — retiré depuis, voir juste en dessous) : jusqu'ici câblé en dur sur `setRoute("home")`, donc systématiquement renvoyé à Accueil quelle que soit la profondeur de navigation dans l'onglet courant (ex. Apprendre → Majeurs → une carte → ce bouton sautait directement à Accueil au lieu de remonter à la grille des Majeurs). Corrigé pour déléguer au bouton « ← Retour » déjà présent sur l'écran (`#detailBack`, quand il existe) — exactement le même mécanisme, pas une nouvelle logique de navigation : `#detailBack.click()` remonte toujours d'un niveau exact, en s'appuyant sur `backTo()`, capturé une seule fois à l'ouverture de CET écran précis (voir le commentaire au-dessus de `showSymbolDetail()`). Seuls les écrans racine d'un onglet (Accueil/Tirage/Apprendre/Rêves/Astro eux-mêmes) n'ont pas de `#detailBack` — pour eux, le bouton continue d'aller à Accueil comme avant.

**"Je veux le logo en haut à gauche (à la place de la flèche qui revient à l'accueil, que tu peux supprimer du coup), un peu plus grand [...] et pas opaque"** : le logo déménage du filigrane flottant bas-gauche décrit ci-dessus vers le coin haut-gauche de la topbar, à la place exacte de `#backBtn` — entièrement retiré du code (`index.html`, et dans `app.js` son `back.hidden` dans `render()` et son `onclick` dans `bind()`) plutôt que simplement masqué, puisque le bouton « ← Retour » de chaque écran (`#detailBack`) fait déjà ce travail de navigation, sans avoir jamais dépendu de `#backBtn`. `.brand-mark-topbar` (renommée depuis `.brand-mark`, qui n'existe donc plus qu'à cet unique endroit) : 38px de large (30px avant), opacité `.6` (`.4` avant — toujours nettement translucide, pas un aplat opaque, mais plus présente qu'un simple filigrane maintenant qu'elle occupe un emplacement structurel de la topbar plutôt qu'un coin flottant) ; `pointer-events:none`/`aria-hidden` inchangés, ce n'est toujours qu'une marque, pas un contrôle cliquable. Comme `#homeBtn` juste à côté, elle vit directement dans `<header class="topbar">` (plutôt que hors de `#app` comme l'ancien filigrane flottant) : la topbar elle-même étant déjà persistante à travers tous les re-rendus de `render()` (seul son contenu textuel change), elle reste visible sur chaque écran sans changement supplémentaire nécessaire.

**"Aligne bien toutes les illustrations de l'onglet arcanes (aligne comme la divinité tutélaire)"** : dans « Arcanes majeurs liés » (`renderPersonalMythology()`), chaque illustration (Soleil/Lune/Ascendant) était enveloppée dans un `<div class="card-grid">` — exactement le même bug déjà corrigé une fois pour la divinité tutélaire juste au-dessus (« La divinité tutélaire est passée à Techné [...] mal centrée », voir plus haut) : `card-grid` (`repeat(auto-fill,minmax(150px,1fr))`) est pensé pour plusieurs cartes côte à côte, et étire/désaxe une carte seule au lieu de la centrer. Ce bug avait ré-apparu ici car le correctif de l'époque n'avait touché que le bloc de la divinité tutélaire, pas celui des arcanes majeurs liés, resté sur son ancien marquage. Corrigé de la même façon : `cardHTML()` appelée directement dans `.detail`, sans wrapper `card-grid`, où `.detail .tarot-card{max-width:270px;margin:10px auto 20px}` s'occupe déjà de centrer et tailler proprement — les 1 à 3 illustrations de l'écran (divinité + jusqu'à 3 arcanes) partagent maintenant exactement le même alignement.

**"Dans la tuile figure mythologique, change le texte car il n'est pas exact (il n'y a pas 104 divinités dans le jeu de tarot)"** : la tuile « Figures mythologiques » d'`apprendre()` disait « Les 104 divinités et héros du jeu » — vrai pour le total de `DEITY_NOTES`, mais trompeur : sur ces 104 figures, seulement 78 sont réellement incarnées par une carte du jeu (`deityFigureHasCard()`, même critère que le calcul de `related` dans `showDeityDetail()`) ; les 26 autres (Jason, Achille, Thétis, Atlas, Psyché…) n'existent que comme contexte mythologique cité dans le `lore` d'une autre fiche — déjà correctement expliqué ailleurs dans l'app (`showLearnFigures()` : « certaines ont leur propre carte, d'autres n'apparaissent que dans une lecture »), mais pas sur cette tuile. Corrigé pour afficher les deux chiffres sans ambiguïté : « 104 divinités et héros, dont 78 incarnent une carte du jeu. »

**Bracelet personnalisé** (retour direct d'utilisatrice, en réponse à la question « comment structurer l'intégration bracelet + site marchand » : "j'imaginais la partie arcanes liés comme gratuite. Comme ça chacun peut générer un bracelet. Par contre les comptes premium auront une réduction sur l'achat du bracelet. Une fois que l'on a tiré ses arcanes liées, on a un bracelet généré. Quand on clique dessus, on est renvoyé sur le site marchand, avec exactement le bracelet qui a été généré (on peut ensuite le remodifier si on aime pas). On doit pouvoir faire pareil pour les proches (sans la description ia par contre)") : `braceletSectionHTML(links, forName, premiumOn, title, buttonLabel)`, une section « Ton bracelet » factorisée, réutilisée à l'identique par `renderPersonalMythology()` (profil principal) et `renderComparison()` (chaque proche, avec juste un titre et un libellé de bouton personnalisés — « Le bracelet de Marc », « Créer le bracelet de Marc »). Trois points structurants :
- **Toujours gratuite**, contrairement au texte explicatif du bloc « Arcanes majeurs liés » juste au-dessus (`premiumLockHTML()`, inchangé) : la recette du bracelet ne dépend que de `majorLinksFor(astral)` — quelles cartes, pas le texte qui les explique — donc aucune raison de la verrouiller. Même chose pour un proche : `majorLinksFor(relation.astral)` calcule SA PROPRE recette (ses propres Soleil/Lune/Ascendant, pas une comparaison avec le profil principal), sans jamais passer par le moindre texte IA — l'infrastructure `majorLinksText`/`ensureAstralText()` n'existe de toute façon que pour le profil principal, donc "sans la description IA" est le comportement naturel ici, pas un filtrage supplémentaire à coder.
- **Premium = réduction, pas déblocage** : `braceletLinkFor(links, forName, discount)` ajoute un paramètre `promo` (avec `BRACELET_PREMIUM_DISCOUNT_CODE`) uniquement quand le compte est premium — la fonctionnalité elle-même reste identique, seul le lien change.
- **Le pont vers la boutique est un simple lien, pas une intégration technique** : `braceletCardSlug()` transforme chaque carte en id court et lisible (« XI — La Force » → `la-force`), `braceletLinkFor()` les assemble dans une URL (`?ref=delphes&cards=la-force,la-lune,l-etoile&for=Léa[&promo=...]`) vers `BRACELET_SHOP_BASE_URL`. Ce champ, comme `BRACELET_PREMIUM_DISCOUNT_CODE`, est un **placeholder explicite** (la boutique elle-même n'existe pas encore, "qui doit être créé") — à remplacer par la vraie URL/le vrai code une fois la boutique en ligne, sans toucher au reste du mécanisme. Aucune donnée personnelle au-delà du prénom choisi n'est transmise (ni date de naissance, ni lieu, ni le reste du thème) ; la personne peut toujours modifier le bracelet une fois sur place, le lien ne fait que le pré-remplir.

**"Créer mon bracelet doit être tout en haut. Enlève le texte explicatif en haut et en dessous. Pareil pour les proches"** : `braceletSectionHTML()` remonte en tout premier dans `renderPersonalMythology()` (juste après le titre "Mythologie personnelle" et sa phrase d'intro, avant même "Ta divinité tutélaire") et dans `renderComparison()` (juste après "Toi & {prénom}", avant le type de relation, le mode premium et le résumé des deux thèmes) — dans les deux cas le tout premier élément d'action de l'écran plutôt qu'enterré en bas. Les deux notes explicatives (« X breloques, gratuites pour tout le monde... » au-dessus, « Le bracelet arrive déjà configuré... » en dessous) disparaissent : la section se réduit au strict nécessaire, un titre et le bouton. Avec elles disparaît aussi la mention explicite de la réduction premium dans le texte (« Ta réduction premium sera appliquée automatiquement ») — la réduction reste bien réelle et appliquée (le paramètre `promo` du lien, inchangé), simplement plus annoncée en toutes lettres à cet endroit.

**Icône d'application** ("Tu peux mettre mon logo en icône d'application, au lieu de l'étoile blanche sur fond terracotta ? Et faire un vrai carré au bord rond, car là c'est un rond donc il y a des bords noirs.") : l'ancienne `icon.svg` (un `<circle r="48">` dans un carré 100×100, avec un simple glyphe ✦ blanc) laissait les 4 coins du carré totalement transparents — un cercle inscrit dans un carré ne couvre que ~78,5% de sa surface (π/4), les ~21,5% restants aux coins n'étaient peints par rien du tout, d'où le noir (ou toute autre couleur de fond) qui y transparaissait selon le lanceur/l'OS. Remplacée par une vraie icône carrée aux coins arrondis (rayon ~22% du côté, un rendu "squircle" proche du style iOS), **remplie de bord à bord** (fond `--terracotta`, sans le moindre pixel transparent en dehors des 4 petites courbes de coin — ~3,8% de la surface, contre ~21,5% pour l'ancien cercle) : `logo-mark.png` (voir plus haut) y est centré et mis à l'échelle (~82% de large, recalé à ~86% de haut vu son format tout en hauteur). Générée en une seule passe Python/Pillow (fond arrondi + logo composé par-dessus) à 1024×1024, puis déclinée en plusieurs fichiers :
- `icon.svg` — le PNG 192×192 encodé en base64 et embarqué dans un `<svg viewBox="0 0 100 100"><image .../></svg>` : garde exactement le même nom de fichier, donc `index.html` (`<link rel="icon">`) n'a rien eu à changer pour ce point précis.
- `icon-192.png` / `icon-512.png` — remplacent l'ancienne entrée unique `"icon.svg", sizes:"any"` de `manifest.json` par les deux tailles standard PNG (192×192, 512×512) que les navigateurs/OS attendent explicitement pour l'installation PWA (bannière d'installation Android/desktop, icône d'écran d'accueil).
- `icon-180.png` — nouveau `<link rel="apple-touch-icon">` dans `index.html` : iOS ne gère pas bien le SVG pour « Ajouter à l'écran d'accueil » (Safari peut retomber sur une capture d'écran de la page en son absence) — sans ce fichier, la correction ci-dessus n'aurait même pas été garantie de s'appliquer sur iPhone, alors que « Installer sur iPhone » (voir plus bas) est un parcours documenté de l'app.

Les 3 nouveaux PNG sont ajoutés à `ASSETS` dans `service-worker.js` (`CACHE` bumpé en conséquence) pour qu'une app déjà installée reçoive bien la nouvelle icône au prochain chargement, comme le reste des fichiers statiques (voir « réseau d'abord, cache seulement en secours hors-ligne » plus haut).

**"Enlève les boutons faire son bracelet pour l'instant. On les mettra quand je serai prête"** : `BRACELET_ENABLED` (nouvelle constante, à côté de `BRACELET_SHOP_BASE_URL`) coupe l'affichage des deux sections bracelet (profil principal et chaque proche) sans toucher au reste du mécanisme — les deux appels à `braceletSectionHTML()` (dans `renderPersonalMythology()` et `renderComparison()`) deviennent `${BRACELET_ENABLED ? braceletSectionHTML(...) : ""}`. Tout ce qui a été construit (recette via `majorLinksFor()`, `braceletCardSlug()`, `braceletLinkFor()`, gratuité totale, réduction premium via le paramètre `promo`, positionnement en haut d'écran, absence de texte explicatif) reste intact et déjà testé — remettre `BRACELET_ENABLED` à `true` suffira à tout réafficher d'un coup, sans rien réécrire ni retester depuis zéro.

**Garde-fou sur les appels IA** ("Met un garde fou pour les tests ia (maximum 10 par personne pour le premium te paraît bien ? Vu que pour la version gratuite c'est déjà limité)") : le mode gratuit a déjà sa propre limite indépendante (1 tirage général par jour, `hasUsedFreeGeneralReadingToday()`, plus le reste du contenu IA déjà verrouillé premium à l'affichage) — ce garde-fou-ci vise spécifiquement le premium, activé par défaut pour tout le monde tant qu'aucun vrai paiement n'existe (voir `isPremiumEnabled()`), sans lequel n'importe quel appareil pouvait déclencher un nombre illimité d'appels IA facturés. `AI_CALL_LIMIT_PER_PERSON = 10` : `enforceAiCallLimit()` (nouvelle fonction) est appelée juste avant CHACUN des 7 appels réseau vers un endpoint IA (`reading`/`portrait`/`astral-text`/`retrospective`/`ritual`/`comparison-text`/`dream` — jamais `astral.js` ni `transits.js`, purement calculatoires, aucun appel IA) ; ne fait rien tant que le compte n'est pas premium, sinon incrémente un compteur local (`delphesAiCallsUsage`, `{month, count}`) et lève une erreur une fois 10 appels atteints. **"On est d'accord 10 appels ia par personne c'est par jour ? Ou par mois ?"** : tranché pour PAR MOIS CIVIL — 10/jour aurait été bien trop généreux pour un simple garde-fou (300/mois), et un total figé pour toujours aurait fini par bloquer définitivement les deux personnes qui testent aujourd'hui, sans date à laquelle elles retrouvent l'usage normal. `currentMonthKey()` (`"AAAA-MM"`) + comparaison de chaîne, même technique que `hasUsedFreeGeneralReadingToday()` juste en dessous mais à l'échelle du mois plutôt que du jour — remise à zéro automatique au changement de mois, sans tâche de fond à programmer. Aucun autre câblage nécessaire : chaque appelant retombe déjà sur son repli hors-ligne habituel pour toute erreur réseau/API (voir par exemple le `catch` de `startAIReading()`), donc cette erreur suit exactement le même chemin, avec un message explicite dans les logs (`console.warn`/`console.error` déjà en place) pour la distinguer d'une vraie panne réseau. Un simple compteur local, comme le reste de l'app (`APP_ACCESS_CODE` protège contre un inconnu qui tomberait sur l'URL, pas contre un usage prolongé par une personne qui a déjà le code) : pas une protection béton contre un abus délibéré (un appareil réinitialisé remet le compteur à zéro), mais un filet suffisant pour une phase de test avec des personnes connues — le même compromis assumé partout ailleurs dans cette app (aucun compte, aucune vraie identité côté serveur).

**"10 par mois c'est pas trop peu pour du premium ? Passe à 50 non ?"** : à raison — 10 était clairement trop serré. L'Horoscope du jour (`ensureRitual()`, déclenché automatiquement à chaque ouverture de l'accueil mais un seul appel réel par jour grâce à son propre cache, `getCachedRitual()`) consomme à lui seul jusqu'à ~30 appels/mois pour quelqu'un qui ouvre l'app tous les jours — l'usage quotidien normal visé par une appli de divination — épuisant tout le quota avant même le premier tirage du mois. `AI_CALL_LIMIT_PER_PERSON` : 10 → 50, qui laisse de la marge pour l'horoscope quotidien plus plusieurs tirages/interprétations de rêve à la demande, tout en restant un vrai garde-fou (le coût réel de 50 appels/mois/personne reste négligeable pour une phase de test avec des personnes connues).

**Paiement premium** ("tu peux préparer le système de paiement, sans l'activer pour l'instant ? Ça sera à 9,99€ par mois. [...] je fais tout de mon téléphone et ça fonctionne très bien. L'autre personne qui teste l'appli aussi le fait de son téléphone") : même esprit que `BRACELET_ENABLED` — tout le mécanisme est construit et testé, mais entièrement invisible tant que `PAYMENT_ENABLED` (nouvelle constante) reste à `false`.
- **Stripe plutôt qu'Apple/Google (StoreKit/Play Billing)** : l'app n'existe aujourd'hui QUE comme PWA (voir « Prête pour l'App Store ? » plus bas) — aucun binaire natif à configurer pour du IAP, donc aucun autre choix réaliste tant que ce chantier n'a pas eu lieu. Stripe Checkout fonctionne directement depuis un navigateur mobile (webview ou Safari/Chrome), exactement l'usage actuel confirmé par l'utilisatrice. Le jour où l'app est emballée nativement (Capacitor), Apple/Google IAP pourra s'ajouter comme un second mode de paiement, sans retirer celui-ci.
- **Aucune base de données ni webhook**, cohérent avec le reste de l'app (aucun compte, tout en `localStorage`) : à la place d'une source de vérité "à nous", Stripe lui-même sert de source de vérité. Le navigateur garde juste l'identifiant de l'abonnement Stripe créé (`delphesSubscription`) et le fait revalider par Stripe directement (`/api/subscription-status`) au plus une fois par jour (`verifySubscriptionStatus()`) — jamais de webhook à sécuriser, jamais de base à maintenir. Contrepartie assumée : un abonnement annulé côté Stripe reste actif localement jusqu'à la prochaine revérification (max 24h), et n'importe qui pourrait en théorie fabriquer un `delphesSubscription` bidon dans son navigateur — exactement le même compromis "confiance côté client" que le reste de l'app ; un vrai serveur de vérité deviendra nécessaire si l'app grandit au-delà d'une phase de test avec des personnes connues.
- **Flux** : `startSubscriptionCheckout()` demande une session Stripe Checkout à `/api/create-checkout-session` (qui crée la session avec le Price `STRIPE_PRICE_ID`, mode `subscription`) et redirige le navigateur dessus — Stripe héberge lui-même la page de paiement, aucune donnée de carte ne transite par le serveur de l'app. Au retour (`?checkout=success&session_id=...` dans l'URL), `checkPendingCheckoutReturn()` (appelée juste avant le tout premier `render()`, jamais avant, même logique que les `ensureX()` du reste de l'app) nettoie l'URL puis confirme le paiement via `/api/checkout-session-status`, qui interroge Stripe et renvoie l'id d'abonnement obtenu.
- **`isPremiumEnabled()`** vérifie désormais `PAYMENT_ENABLED` en premier : tant qu'il reste à `false` (aujourd'hui), le comportement est identique au caractère près à avant ce changement (l'interrupteur local `delphesPremium`) — aucune régression pour les deux personnes qui testent déjà l'app. Une fois activé, il bascule entièrement sur `hasActiveSubscription()` (le statut Stripe mis en cache localement) ; l'interrupteur local de test (case « Mode premium (aperçu local — aucun vrai paiement) », toujours présente dans Profil astral/comparaisons) continuera d'exister pour l'aperçu manuel, mais n'aura plus d'effet une fois le paiement actif — un futur nettoyage à prévoir à ce moment-là plutôt que maintenant.
- **Bouton « Passer premium — 9,99 €/mois »** : ajouté dans `renderProfilResults()` (Profil astral), juste au-dessus de la case « Mode premium », entièrement conditionné à `${PAYMENT_ENABLED ? ... : ""}` — absent du HTML rendu aujourd'hui, donc rien à voir ni à cliquer tant que le drapeau n'est pas activé.
- **À configurer sur Vercel** avant tout test, même drapeau désactivé (les 3 endpoints répondent sinon une erreur 500 propre, jamais un plantage — vérifié explicitement par les tests) : `STRIPE_SECRET_KEY` (clé secrète Stripe, Tableau de bord → Développeurs → Clés API) et `STRIPE_PRICE_ID` (id du Price Stripe pour l'abonnement à 9,99€/mois, à créer dans Stripe avant de renseigner cette variable — Produits → + Ajouter un produit → récurrent mensuel).

**"Ajoute une fiche sur Hygie, Médée, Calliope, Cassiopé, Mélanoé, celui qui a été transformé en chouette (Athena a récupéré après) au enfer pour avoir dit que Perséphone avait mangé une grenade, Pasiphae"** : 7 nouvelles figures dans `DEITY_NOTES`/`DEITY_LORE` (104 → 111), toutes déjà citées en passant dans d'autres fiches avant cette demande (Médée dans « Jason », Cassiopée dans « Andromède », Pasiphaé dans « Minos »/« Ariane »/« Hélios »/« Taureau »/« Labyrinthe »…) mais sans fiche propre jusqu'ici — même situation que Jason ou Achille en leur temps. `Hygie` (fille d'Asclépios, la « coupe d'Hygie » toujours symbole de la pharmacie aujourd'hui), `Médée` (magicienne petite-fille d'Hélios, alliée puis ennemie jurée de Jason), `Calliope` (Muse de la poésie épique, mère d'Orphée), `Cassiopée` (reine vaniteuse, mère d'Andromède, punie par une constellation qui tourne la tête en bas), `Pasiphaé` (épouse de Minos, mère du Minotaure). Chaque fiche existante qui la citait déjà a reçu en retour un renvoi `(voir la fiche « X »)` vers la nouvelle fiche dédiée (`jason`, `andromède`, `minos`, `ariane`, `hélios`, `orphée`, `circé`, `taureau`, `labyrinthe`) ; les symboles `serpent` (Hygie), `grenade` (Ascalaphos, voir plus bas) et `étoiles` (Cassiopée) ont reçu un nouveau paragraphe et une entrée dans leurs `links`.
- **« Mélinoé »** (l'utilisatrice écrivait « Mélanoé ») : déesse orphique des fantômes et des terreurs nocturnes, née d'une ruse de Zeus qui aurait pris l'apparence d'Hadès pour s'unir à Perséphone — le nom mythologique réel a été utilisé plutôt que l'orthographe approximative, aucune figure « Mélanoé » distincte n'existant dans la mythologie grecque.
- **« Celui qui a été transformé en chouette [...] pour avoir dit que Perséphone avait mangé une grenade »**, identifié comme **Ascalaphos** : gardien du jardin des Enfers, il rapporte avoir vu Perséphone manger des grains de grenade — le geste qui la lie irrévocablement au monde souterrain (voir « Grenade » et « Perséphone », toutes deux mises à jour en retour) — et se voit changé en chouette par vengeance. Le détail « Athéna a récupéré après » ne correspond en réalité à aucune version classique du mythe d'Ascalaphos (chez Ovide, c'est Perséphone elle-même qui le change en chouette ; chez Apollodore, c'est Déméter qui l'écrase sous un rocher, dont Héraclès le libère plus tard) : il s'agit très probablement d'une confusion avec un mythe distinct, celui de Nyctimène, changée en chouette par Athéna par compassion et recueillie parmi ses oiseaux sacrés — les deux histoires ne se rejoignant que par l'oiseau qu'elles ont en commun. La fiche d'Ascalaphos explique cette distinction explicitement plutôt que de fusionner silencieusement les deux mythes, pour rester exacte tout en respectant ce dont l'utilisatrice se souvenait.
- Aucune de ces 7 figures n'a de carte associée (comme les 26 déjà citées avant elles) : `deityFigureHasCard()` reste `false` pour toutes, aucun paragraphe « Dans le Tarot » n'a été fabriqué de toutes pièces. La tuile « Figures mythologiques » (voir « Dans la tuile figure mythologique… » plus haut) affiche désormais 111 divinités et héros, dont toujours 78 incarnant une carte du jeu — sans qu'aucun code n'ait eu besoin d'être modifié pour ça, ces deux chiffres étant déjà calculés dynamiquement.

**"Oui mais la chouette qui accompagne Athéna est Ascalaphos il me semble, non ? [...] pour moi [Mélinoé] est la fille d'Hadès et Perséphone, [...] une moitié noire et une moitié blanche"** : contestation directe des deux fiches ci-dessus, vérifiée contre les sources classiques (Ovide *Métamorphoses* V et II, Apollodore *Bibliothèque* 1.5.3 et 2.5.12, Hymne orphique 71) avant de trancher plutôt que de céder ou de camper sur la version initiale sans vérifier.
- **Ascalaphos/Athéna** : la fiche initiale était correcte, confirmée par les trois sources croisées — jamais Athéna dans le mythe d'Ascalaphos (ni chez Ovide, ni chez Apollodore, ni dans le fragment d'Euphorion de Chalcis, tous cités par Theoi.com). La chouette adoptée par compassion appartient bien à un mythe entièrement différent, celui de Nyctimène (Ovide, *Métamorphoses* II) — confirmé. La fiche a quand même été affinée : la transformation en chouette était auparavant attribuée à une source vague (« selon Ovide », sans agent précis) alors qu'Apollodore l'attribue explicitement à Déméter elle-même (après qu'Héraclès a soulevé le rocher) — corrigé pour nommer clairement les DEUX traditions parallèles et cohérentes en elles-mêmes plutôt qu'un hybride mal attribué : Déméter chez Apollodore (rocher, puis chouette après la libération par Héraclès), Perséphone chez Ovide (chouette directe, sans rocher ni Héraclès).
- **Mélinoé** : ici, la fiche initiale contenait une vraie erreur, repérée grâce à la contestation. L'Hymne orphique 71 confond deux détails distincts que la fiche avait fusionnés à tort en un seul (« mi-noir, mi-couleur safran ») : Mélinoé y est décrite « vêtue de safran » (une épithète sur son voile, comme pour Éos) ET, séparément, avec des membres « mi-noirs, mi-blancs » (traduisant sa double origine, sombre et lumineuse) — corrigé pour distinguer clairement les deux. Sur la filiation, l'hymne source dit bien que Zeus prend l'apparence d'Hadès pour s'unir à Perséphone (ni Taylor ni Athanassakis/Wolkow, les deux traductions de référence, ne la disent simplement « fille d'Hadès ») — mais la version que l'utilisatrice connaît (fille directe d'Hadès et Perséphone) est une simplification si répandue dans les sources modernes (Wikipédia, sites de mythologie grand public) qu'elle mérite d'être mentionnée comme telle plutôt qu'ignorée : la fiche garde la version de la source primaire tout en signalant explicitement cette simplification courante et sa raison d'être (Zeus et Hadès parfois vus comme deux visages d'une même puissance dans la religion orphique).

**"Enrichi les fiches suivantes ; Aphrodite : liaison avec hermes dont naît hermaphrodite et avec Dionysos dont naît priape. Arès : père des Amazones. Fais fiches suivantes : Hermaphrodite, Priape, Antiope, Thesée, Hippolyte, Didon, Erichtonios, alcippé, Enyo (ne pas présenter comme fille d'Arès)"** : 9 nouvelles figures dans `DEITY_NOTES`/`DEITY_LORE` (111 → 120), vérifiées avant rédaction contre les sources classiques (Ovide *Métamorphoses* 4 et 15, Diodore de Sicile *Bibliothèque* 4.6, Pseudo-Hygin *Fables* 160/225, Apollodore *Bibliothèque* 3.14.2 et 3.14.6, Plutarque *Vie de Thésée* 26-29, Euripide *Hippolyte* et *Électre*, Virgile *Énéide* 4, Justin *Épitomé de Trogue Pompée* 18, Hésiode *Théogonie*, Homère *Iliade*, Quintus de Smyrne *Posthomerica*).
- **Aphrodite** enrichie de deux liaisons plus brèves : avec Hermès, dont naît Hermaphrodite (voir sa fiche), et avec Dionysos, lors d'une réconciliation passagère après sa passion pour Adonis, dont naît Priape (voir sa fiche) — les deux mentions réciproques ont aussi été ajoutées aux fiches déjà existantes d'Hermès et de Dionysos.
- **Arès** enrichi du rôle de père spirituel des Amazones : leur première reine, Otrera, en aurait été l'épouse mortelle — Arès devient ainsi le patron de la nation guerrière tout entière plutôt que le père littéral de chacune de ses reines. Une seule Amazone porte pourtant, dans les textes anciens (Quintus de Smyrne), le titre exact de « fille d'Arès » : Penthésilée, nommée explicitement pour éviter de généraliser à tort ce titre à toutes les Amazones. Un deuxième ajout couvre sa fille mortelle Alcippé et son tout premier procès (voir plus bas).
- **Hermaphrodite** et **Priape** : nouvelles fiches. Hermaphrodite, fils d'Hermès et d'Aphrodite, doit son nom à la fusion des deux — celle de son propre corps avec celui de la naïade Salmacis, qui l'enlaça de force en suppliant les dieux de ne jamais les séparer (Ovide, *Métamorphoses* 4.271-388) ; la malédiction de la source qui en résulte (quiconque s'y baigne en ressort « efféminé ») est incluse. Priape, selon Diodore de Sicile fils de Dionysos et d'Aphrodite (Pseudo-Hygin donne une tradition plus rare, fils d'Hermès), frappé de laideur difforme par la jalousie d'Héra — son nom a donné le mot médical « priapisme ».
- **Antiope** et **Hippolyte** : la mythologie ancienne elle-même est incohérente ici, et la fiche le dit plutôt que de trancher artificiellement. Plutarque rapporte que ses propres sources ne s'accordent pas sur le nom de l'Amazone que Thésée épousa : Bion l'appelle Antiope, Cleidémos l'appelle Hippolyte — la même reine dont Héraclès obtient par ailleurs la ceinture dans un tout autre épisode (déjà citée dans la fiche « Héraclès », qui reçoit désormais un renvoi). Plutôt que de forcer une généalogie unique, la fiche « Hippolyte » assume le doublon en présentant clairement les deux figures homonymes sans lien de parenté (la reine amazone, et le fils de Thésée voué à Artémis, victime d'Aphrodite et de la fausse accusation de Phèdre — Euripide, *Hippolyte*) ; la fiche « Antiope » distingue en plus l'Antiope amazone de l'Antiope thébaine, mère d'Amphion et Zéthos, sans rapport avec les Amazones.
- **Thésée** : nouvelle fiche complète (double paternité Égée/Poséidon, l'épreuve du rocher, le chemin semé de brigands vers Athènes déjà cité dans « Chemin », le Minotaure et Ariane déjà cités dans « Labyrinthe », l'épisode amazone avec Antiope, puis Phèdre et la tragédie d'Hippolyte) — toutes les fiches qui le citaient déjà en passant (Chemin, Labyrinthe, Ariane, Dionysos) ont reçu un renvoi en retour.
- **Didon** : nouvelle fiche, déjà citée dans « Énée » qui reçoit un renvoi en retour. Les deux traditions y sont présentées honnêtement : la romance avec Énée (Virgile, *Énéide* 4) est la plus connue, mais une tradition antérieure (l'historien Justin, d'après Timée) l'ignore complètement — Didon s'y donne la mort pour rester fidèle au souvenir de son défunt mari plutôt que d'épouser le roi numide Iarbas, sans aucun lien avec Énée, chronologiquement plus cohérent puisque la fondation de Carthage est postérieure de plusieurs siècles à la guerre de Troie.
- **Érichthonios** : nouvelle fiche, déjà cité en passant dans « Héphaïstos », « Athéna » et « Corbeau », qui reçoivent chacune un renvoi en retour. Point de précision important confirmé par Apollodore (*Bibliothèque* 3.14.6) : c'est Gaïa, et non Athéna, qui est fécondée par la semence d'Héphaïstos tombée au sol — Athéna n'est jamais sa mère biologique, seulement son adoptante. L'invention du char à quatre chevaux est signalée comme un ajout plus tardif (Ératosthène, *Catastérismes*), distinct du mythe fondateur d'Apollodore.
- **Alcippé** : nouvelle fiche — fille d'Arès et d'Aglauros (la même Aglauros qui ouvre plus tard le coffre d'Érichthonios, renvoi ajouté entre les deux fiches). Son agression par Halirrhothios, fils de Poséidon, et le procès qui s'ensuit devant les douze dieux de l'Olympe sont à l'origine du nom de l'Aréopage (Apollodore 3.14.2, Euripide *Électre* 1258-1263) — présenté comme le premier procès opposant un dieu à un autre devant leurs pairs, distinct et non contradictoire avec le procès humain d'Oreste déjà décrit dans la fiche « Érinyes » comme le premier tribunal remplaçant leur vengeance.
- **Enyo**, le point le plus délicat de cette demande : l'utilisatrice a explicitement demandé de ne pas la présenter comme fille d'Arès, et la vérification confirme qu'elle a raison — aucune source antique (Homère, Hésiode, Quintus de Smyrne, Pausanias) ne fait d'Enyo la fille d'Arès ; c'est une invention moderne (jeux vidéo, comics) sans fondement classique. Elle est plutôt sa compagne de combat, une « pilleuse de villes » (Homère, *Iliade* 5), avec un culte réel attesté par Pausanias (statue dans le sanctuaire d'Arès à Athènes, aux côtés d'Athéna et d'Aphrodite). Le lien de parenté avec Arès le mieux attesté revient en réalité à Éris, la Discorde, que l'Iliade appelle explicitement « sœur et compagne » du dieu de la guerre — les deux déesses se confondant souvent. La fiche distingue aussi cette Enyo guerrière d'une seconde Enyo, sans rapport, l'une des trois Grées filles de Phorcys et Céto (Hésiode, *Théogonie* 270-273).
- Aucune de ces 9 figures n'a de carte associée : `deityFigureHasCard()` reste `false` pour toutes, aucun paragraphe « Dans le Tarot » n'a été fabriqué de toutes pièces. La tuile « Figures mythologiques » affiche désormais 120 divinités et héros sans qu'aucun code n'ait eu besoin d'être modifié, ce chiffre étant déjà calculé dynamiquement.

**"Fais les Hespérides, Hersé, les muses (rivalité avec les piérides, etc) et fais une fiche pour chaque muse (pour celles qui n'en n'ont pas déjà). Refais la fiche d'Enyo, tu insistes trop lourdement sur le fait que ce ne soit pas sa fille, ce n'est pas très intéressant. Tu peux plutôt décrire comment elle était perçue par exemple."** : 11 nouvelles figures dans `DEITY_NOTES`/`DEITY_LORE` (120 → 131), vérifiées avant rédaction (Hésiode *Théogonie*, Apollodore *Bibliothèque* 2.5.11/3.14.3, Ovide *Métamorphoses* 2 et 5, Hyginus *Fables*, Homère *Iliade*, Nonnos *Dionysiaca*, Quintus de Smyrne *Posthomerica*, Pausanias 1.8.4).
- **Hespérides** : nouvelle fiche. Point d'exactitude assumé plutôt qu'ignoré — Hésiode, la source la plus ancienne, les dit filles de la Nuit seule, sans père (*Théogonie* 215-216) ; la généalogie « filles d'Atlas », bien plus répandue aujourd'hui et déjà utilisée telle quelle dans la fiche « Atlas » existante, n'apparaît que dans des sources plus tardives — la fiche garde donc les deux, en signalant laquelle est la plus ancienne, plutôt que de trancher ou d'ignorer la version déjà présente ailleurs dans l'appli. Gardiennes du pommier d'or (cadeau de noces de Gaïa à Héra) aux côtés du dragon Ladon ; onzième travail d'Héraclès (Atlas cueille les pommes pendant qu'Héraclès porte le ciel, Apollodore 2.5.11) — renvois ajoutés aux fiches « Atlas » et « Héraclès » qui la citaient déjà.
- **Hersé** : nouvelle fiche — sœur d'Aglauros et Pandrosos (déjà citées dans « Érichthonios », qui reçoit un renvoi), aimée d'Hermès lors de la procession des Panathénées (Ovide, *Métamorphoses* 2.708-832) ; Aglauros, rongée de jalousie par l'Envie qu'Athéna lui envoie, tente de barrer le passage à Hermès et se voit changée en statue de pierre. Le fils qu'elle aurait eu d'Hermès, Céphale, est signalé avec la nuance que les sources anciennes elles-mêmes (Apollodore, Hyginus) mêlent ce Céphale à l'autre Céphale plus connu, fils de Déion et époux de Procris, sans jamais les démêler complètement — renvoi ajouté à la fiche « Hermès » existante.
- **Muses** : nouvelle fiche collective (parenté Zeus/Mnémosyne, mont Hélicon et source Hippocrène, Parnasse et source Castalie, Apollon Musagète — renvoi ajouté à sa fiche existante) plus la rivalité demandée avec les Piérides : les neuf filles de Piéros les défient en un concours de chant jugé par des nymphes, Calliope répond pour les Muses par le récit du rapt de Perséphone (renvoi ajouté à la fiche « Calliope » existante, qui gagne au passage cet épisode), les Piérides perdent et sont changées en pies pour avoir refusé leur défaite (Ovide, *Métamorphoses* 5.294-678). Point signalé : la répartition en neuf arts distincts, si familière aujourd'hui, est une systématisation plus tardive — Hésiode se contente de les nommer sans leur attribuer de domaine.
- **Une fiche par Muse sans fiche existante** (Calliope en avait déjà une) : `Clio` (Histoire), `Euterpe` (musique/aulos), `Thalie` (comédie — homonyme de la Charite Thalia déjà présente, distinguée explicitement, renvoi ajouté dans les deux sens), `Melpomène` (tragédie, forme avec Thalie le couple des masques du théâtre), `Terpsichore` (danse, parfois dite mère des Sirènes avec Achéloos — ascendance que les sources anciennes hésitent déjà à lui attribuer en propre), `Érato` (poésie amoureuse, invoquée pourtant par Virgile pour le chant guerrier de l'Énéide), `Polymnie` (hymnes sacrés, domaine élargi plus tard à la pantomime), `Uranie` (astronomie, homonyme d'une épiclèse d'Aphrodite sans lien de parenté).
- **Enyo refaite** : la remarque était juste — la version précédente répétait « pas sa fille » dans quasiment chaque paragraphe, ce qui n'apportait rien de plus après la première mention. La nuance sur la parenté est conservée mais réduite à une seule phrase brève, et le reste de la fiche développe sa perception réelle : l'épithète homérique « ptoliporthos », pilleuse de villes (*Iliade* 5) ; chez Nonnos de Panopolis, une frénésie qui attise les deux camps sans jamais en favoriser un ; chez Quintus de Smyrne, « l'effroyable Enyo, aux membres tout éclaboussés de sang » ; son culte réel (statue par les fils de Praxitèle dans le sanctuaire d'Arès à Athènes, Pausanias 1.8.4) ; et son identification romaine à Bellone, dont le « jour du sang » et le rôle dans la déclaration officielle de guerre sont mentionnés comme un contraste éclairant plutôt qu'une simple note en passant.
- Aucune de ces 11 figures n'a de carte associée, même règle que d'habitude. La tuile « Figures mythologiques » affiche désormais 131 divinités et héros, toujours calculé dynamiquement.

**"Cherche tous les dieux qui ont des épithètes, et intègre les à leur fiche personnelle. Intègre ces photos dans les fiches concernées (juste après leur nom). Dans l'ordre: hécate, Artémis, Aphrodite, Persephone, Déméter"** — deux demandes distinctes, traitées séparément.
- **Portraits** (`DEITY_PORTRAITS` dans `app.js`, `showDeityDetail()`) : les 5 images fournies ont été redimensionnées et compressées (700px de large, JPEG qualité 78 — de 2,1-2,7 Mo chacune à l'origine à 77-135 Ko, comparable aux autres illustrations déjà présentes dans `assets/`) puis placées dans `assets/deity-hecate.jpg`, `deity-artemis.jpg`, `deity-aphrodite.jpg`, `deity-persephone.jpg`, `deity-demeter.jpg`. Elles s'affichent juste après le `<h2>` du nom, avant même la note courte, exactement comme demandé — et seulement pour ces 5 figures : le reste des 131 fiches continue de s'afficher sans image, sans qu'aucune "case vide" ne soit réservée pour elles. Nouvelle classe `.deity-portrait` dans `styles.css`, cohérente avec le style déjà utilisé pour les cartes (bordure dorée, coin arrondi, ombre).
- **Épithètes** : un paragraphe ajouté à chacun des 16 grands dieux déjà présents dans l'appli (Zeus, Héra, Poséidon, Déméter, Athéna, Apollon, Artémis, Arès, Aphrodite, Héphaïstos, Hermès, Hestia, Dionysos, Hadès, Perséphone, Hécate), vérifié avant rédaction contre Homère, Hésiode, Pausanias, Platon, Euripide, Virgile et Apollonios de Rhodes — 2 à 4 épithètes par figure, choisies pour leur intérêt (liées à un lieu, un mythe précis ou une fonction distinctive) plutôt que les plus génériques, et honnêtement nuancées quand la source est plus incertaine (Héphaïstos/Cabires, Hécate/Sotira — cette dernière surtout attestée dans les Hymnes orphiques, bien postérieurs à l'époque classique) plutôt que présentées avec la même assurance que les mieux documentées. Plusieurs épithètes recoupent et enrichissent des fiches déjà existantes plutôt que de rester isolées : Zeus Ctésios renvoie vers la fiche « Ctésios » déjà présente ; Athéna Ergané (patronne des artisans) renvoie vers « Araignée » (Arachné) ; Arès Enyalios renvoie vers « Enyo », une tradition tardive en ayant fait le fils du couple ; Aphrodite Areia (vénérée en armes à Sparte) renvoie vers « Arès » ; Hadès Klymenos, cultivé à Hermione, renvoie vers « Héraclès » (le lieu où l'on situait sa remontée avec Cerbère) ; et Déméter et Perséphone se renvoient mutuellement autour du mythe arcadien de Thelpusa (Poséidon poursuivant Déméter changée en jument) qui explique à la fois l'épithète Erinys de Déméter et la déesse Despoina, à l'origine distincte de Perséphone mais tardivement confondue avec elle. Chaque paragraphe est placé juste après le contenu narratif déjà présent, avant le paragraphe « Dans le Tarot » pour les 8 figures qui en ont un (Artémis, Arès, Aphrodite, Héphaïstos, Hermès, Hadès, Perséphone, Hécate) — les 8 autres (Zeus, Héra, Poséidon, Déméter, Athéna, Apollon, Hestia, Dionysos) n'étant reliées à aucune carte, le paragraphe vient simplement clore leur fiche. Cas particulier : Hestia n'a presque aucune épithète de culte propre — la fiche le dit explicitement plutôt que d'en inventer une, et explique à la place sa formule rituelle unique (invoquée en premier et en dernier dans toute libation).

**"Ajoute ces photos. Dans l'ordre: Mélinoé, Hestia, Hera, Athena, Dionysos"** : même mécanisme que la première série de portraits (`DEITY_PORTRAITS`), simplement complété plutôt que réécrit. Les 5 nouvelles images ont subi le même traitement (redimensionnées à 700px de large, JPEG qualité 78 : 95-159 Ko chacune) et sont placées dans `assets/deity-melinoe.jpg`, `deity-hestia.jpg`, `deity-hera.jpg`, `deity-athena.jpg`, `deity-dionysos.jpg`. `DEITY_PORTRAITS` compte donc désormais 10 figures illustrées sur 131 ; aucun autre code n'a eu besoin d'être modifié, `showDeityDetail()` et la classe `.deity-portrait` étant déjà génériques depuis la première série.

**"Thémis, Hephaistos, Thalie (muse), Terpsichore, Clio. Enlève le « figure mythologique » en dessous des images"** puis **"Pour Thalie il faudra aussi changer le texte très répétitif avec le fait qu'elle n'a pas de lien de parenté avec Thalia (surtout dans la description)"** — trois changements distincts.
- **Troisième série de portraits** : mêmes traitement et mécanisme que les deux précédentes (700px, JPEG qualité 78, 106-142 Ko chacune) — `assets/deity-themis.jpg`, `deity-hephaistos.jpg`, `deity-thalie.jpg`, `deity-terpsichore.jpg`, `deity-clio.jpg`. `DEITY_PORTRAITS` compte désormais 15 figures illustrées sur 131.
- **Retrait du label "Figure mythologique"** : supprimé de `showDeityDetail()`, pour toutes les fiches (pas seulement celles avec un portrait) — c'était affiché juste après le nom (et donc juste après l'image, quand il y en a une), avant la note courte. Seul ce label disparaît : les libellés équivalents des autres écrans de détail (catégorie d'un symbole, « Grammaire des nombres » pour une carte numérale) n'ont pas été touchés, la demande ne portant que sur les fiches de figures.
- **Thalie dé-répétée** : la remarque était juste — la note courte (`DEITY_NOTES`) commençait systématiquement par rappeler l'absence de lien de parenté avec Thalia la Charite avant même de dire son domaine, et le premier paragraphe du mythe (`DEITY_LORE`) répétait la même précision dès sa première phrase. La note courte ne mentionne plus du tout l'homonymie (elle décrit directement son domaine et ses attributs) ; le mythe garde la précision, mais reléguée à un troisième paragraphe court et dédié à la toute fin, une seule fois plutôt que dès l'ouverture de la fiche — la fiche de Thalia (la Charite) n'a pas eu besoin d'être retouchée, elle ne le mentionnait déjà qu'une seule fois.

**"Erato, polymnie, uranie, melpomene, Euterpe"** : quatrième série de portraits, même mécanisme et même traitement que les précédentes (700px de large, JPEG qualité 78 : 110-127 Ko chacune) — `assets/deity-erato.jpg`, `deity-polymnie.jpg`, `deity-uranie.jpg`, `deity-melpomene.jpg`, `deity-euterpe.jpg`. `DEITY_PORTRAITS` compte désormais 20 figures illustrées sur 131 — les neuf Muses sont ainsi toutes illustrées, à l'exception de Calliope.

**"Calliope, Éros et Psyché (à mettre dans les deux), Enée, Dionysos et Ariane (à mettre dans Ariane), Harmonie et Cadmos (à mettre dans les deux)"** : cinquième série, mais seulement 5 images pour 7 figures cette fois — deux portraits représentent un couple et se retrouvent partagés entre deux entrées de `DEITY_PORTRAITS` plutôt que dupliqués en fichier : `deity-eros-psyche.jpg` sert à la fois à `éros` et à `psyché`, `deity-harmonie-cadmos.jpg` à la fois à `harmonie` et à `cadmos`. Le portrait de Dionysos et Ariane suit une règle différente, respectée à la lettre : il ne va que dans la fiche `ariane` (`deity-dionysos-ariane.jpg`) — Dionysos, qui avait déjà son propre portrait individuel depuis une série précédente, le garde inchangé plutôt que de se voir remplacé par celui du couple. Reste : `deity-calliope.jpg` et `deity-enee.jpg`, chacun pour une seule figure. `DEITY_PORTRAITS` compte désormais 27 figures illustrées sur 131 (700px, JPEG qualité 78, 98-139 Ko par fichier).

**"Je viens de lire Dionysos, et il y a toujours écrit figure mythologique en dessous de son image. Tu peux mettre la photo de lui et Ariane juste au dessus du texte qui la mentionne (toujours sur la fiche de Dionysos)? Mais en plus petit, pour pas prendre trop de place."** — deux points.
- **Le label "Figure mythologique"** : vérifié directement dans le code déployé en production au moment du signalement (récupéré via `curl` et exécuté à travers `showDeityDetail("dionysos")`) — il n'y est déjà plus depuis son retrait précédent. Le signalement tenait donc très probablement à un cache d'appareil non rafraîchi (le Service Worker sert la dernière version dès qu'il a fini de se mettre à jour en arrière-plan, mais un onglet resté ouvert depuis avant peut continuer d'afficher l'ancienne page tant qu'il n'est pas rechargé) plutôt qu'à une régression du code — rien n'avait besoin d'être corrigé de ce côté.
- **Portrait de couple inséré dans le texte** : nouveau mécanisme générique, `DEITY_INLINE_PORTRAITS` (distinct de `DEITY_PORTRAITS`, qui n'affiche qu'un seul portrait en tête de fiche) — associe à une figure une ou plusieurs images à insérer au fil du mythe, chacune juste avant le paragraphe qui mentionne un mot-clé donné (ici `dionysos` → le paragraphe contenant « Ariane »). Le fichier réutilisé est exactement celui déjà utilisé sur la fiche d'Ariane (`deity-dionysos-ariane.jpg`), sans nouvel upload — mais affiché nettement plus petit (nouvelle classe `.deity-portrait-inline` dans `styles.css`, ~150px de large contre 280px pour le portrait principal `.deity-portrait`), pour ne pas prendre toute la largeur au milieu du texte comme demandé. Le portrait principal de Dionysos (son propre portrait individuel) et la fiche d'Ariane elle-même restent inchangés.

**"Voici, dans l'ordre, les Heures, les parques, Apollon et Daphné (à intégrer dans la fiche Apollon, au dessus du paragraphe qui mentionne Daphné, en un peu plus petit pour pas prendre trop de place), et Aphrodite qui sauve Enée (à intégrer dans la fiche d'Aphrodite, au dessus d'un paragraphe que tu vas créer pour expliquer pourquoi elle a dû le sauver pendant une bataille à Troie)."** : 4 images, 4 traitements différents, vérifiés avant rédaction (Hésiode *Théogonie*, Homère *Iliade* 5 et 8, Platon *République* 617c).
- **Les Heures** : nouvelle fiche (portrait principal, `DEITY_PORTRAITS`) — filles de Zeus et de Thémis (Eunomia, Dikè, Eiréné), gardiennes de l'ordre saisonnier et moral selon Hésiode ; chez Homère, gardiennes concrètes des portes de l'Olympe, écartant le nuage qui en tient lieu. Renvois ajoutés dans les deux sens : Thémis (leur mère), Charites (compagnes de cortège) et Aphrodite (dont le premier paragraphe précise désormais qu'elles l'accueillirent et la parèrent à son arrivée sur le rivage).
- **Les Parques** (Moires) : nouvelle fiche (portrait principal) — Clotho file, Lachésis mesure, Atropos tranche. Point d'exactitude assumé plutôt que lissé : Hésiode lui-même les fait naître deux fois dans sa *Théogonie*, sans jamais harmoniser les deux généalogies (filles de la Nuit seule dans le passage le plus ancien ; filles de Zeus et de Thémis, sœurs des Heures, dans un passage plus tardif du même poème) — la fiche présente les deux plutôt que d'en choisir une arbitrairement, avec en plus la tradition orphique qui en fait les filles d'Ananké (déjà citées dans sa fiche existante, qui reçoit un renvoi en retour) et l'épiclèse Zeus Moiragète.
- **Apollon et Daphné** : la fiche d'Apollon ne racontait en réalité pas encore ce mythe (seules les fiches « Éros » et « Laurier » le faisaient) — un nouveau paragraphe l'ajoute de son propre point de vue, avec renvois vers les deux, et le portrait s'affiche juste au-dessus en plus petit (`DEITY_INLINE_PORTRAITS`, même classe `.deity-portrait-inline` que pour Dionysos/Ariane — seule l'image source est un peu plus légère, 500px de large au lieu de 700px, puisqu'elle n'est de toute façon jamais affichée en grand).
- **Aphrodite qui sauve Énée** : nouveau paragraphe créé pour expliquer précisément l'épisode (*Iliade* 5) — Énée blessé par le héros grec Diomède, Aphrodite se précipitant pour le protéger de son voile, blessée elle-même au poignet par Diomède (encouragé par Athéna), contrainte de lâcher son fils recueilli par Apollon, repli sur l'Olympe auprès de sa mère Dioné sous les moqueries d'Héra et d'Athéna, et le rappel de Zeus que la guerre n'est pas son domaine. Le portrait s'affiche juste au-dessus, en plus petit également. La fiche d'Énée, qui mentionnait déjà vaguement une mère « protégeant à plusieurs reprises » son fils, précise désormais explicitement cet épisode avec renvoi.
- Aucune des 2 nouvelles figures (Heures, Parques) n'a de carte associée, même règle que d'habitude. La tuile « Figures mythologiques » affiche désormais 133 divinités et héros.

**"Au lieu de mentionner « voir fiche untel » je veux que chaque nom devienne cliquable pour que l'on tombe directement sur sa fiche. Ça allègera le texte, et ça facilitera les recherches."** — environ 360 citations "(voir la fiche « X »)" réparties dans tout le contenu (`DEITY_LORE`, `SYMBOL_LIBRARY.lore`, `CARD_LORE.myth`), bien trop pour être réécrites une par une à la main : un mécanisme générique au rendu, `linkifyLore()`/`LORE_LINK_TARGETS`, s'en charge automatiquement — sans avoir eu besoin de retoucher le contenu déjà écrit, à une quinzaine de phrases près (voir plus bas).
- **Principe** : `LORE_LINK_TARGETS` construit une table nom -> cible (figure ou symbole) à partir de `DEITY_NOTES` et `SYMBOL_LIBRARY` déjà existants. Au rendu, `linkifyLore(texte)` repère chaque citation "(voir la fiche/les fiches « A »[, « B »][ et « C »])", retrouve la dernière occurrence littérale de chaque nom cité juste avant elle dans le même paragraphe (recherche insensible à la casse et aux accents, mot isolé — jamais une coïncidence à l'intérieur d'un autre mot, ex. le symbole « Mer » ne doit jamais matcher dans « commercer »), transforme cette occurrence en lien cliquable (`<span class="clickable-deity">`/`<span class="clickable-symbol">`, même mécanisme de classe et de liaison au clic — `bindChips()` — que les liens déjà existants ailleurs dans l'appli), puis retire la citation entière. Tolère un simple "s" de pluriel français (ex. « Forêt » retrouvé dans « forêts »), en l'incluant dans le lien plutôt que de le laisser seul juste après.
- **Tout ou rien par citation** : si un seul des noms qu'elle cite ne se résout pas (absent de `LORE_LINK_TARGETS`, ou introuvable littéralement juste avant), la citation entière reste intacte plutôt que de risquer de perdre un renvoi en silence — mieux vaut un renvoi non cliquable qu'un renvoi disparu. Vérifié par une analyse exhaustive (script dédié, pas un smoke test) sur les ~360 citations réelles de l'appli : 329 se résolvaient déjà du premier coup, 5 de plus après avoir ajouté la tolérance du pluriel — restaient alors 26 phrases où le nom cité n'apparaissait pas littéralement juste avant (synonyme, adjectif au lieu du nom, énumération générique, ou le nom seulement mentionné dans un *autre* paragraphe — `linkifyLore()` ne regarde jamais au-delà du paragraphe en cours). Chacune a été ajustée à la main, une courte clause supplémentaire ajoutant le mot manquant plutôt qu'une réécriture — ex. « riant Deimos, la Terreur, avec qui il escorte son père au combat » devient « ... au combat, aux côtés de Palioxis, qui incarne le repli de ce même cortège » pour que « Palioxis » soit là où sa citation l'attend. Deux corrections ont aussi révélé et corrigé une vraie coquille préexistante (les fiches de Charites Euphrosyne et Aglaé disaient par erreur « Thalie », la Muse, au lieu de « Thalia », leur sœur Charite).
- **Rendu** : `showDeityDetail()`, `showSymbolDetail()` et `showDetail()` (fiche d'une carte, aperçu compris) appellent désormais `linkifyLore()` au lieu du simple `escapeHTML()` sur le texte du mythe — seul le champ `marseille` des cartes (jamais de citation dedans) et les textes générés par l'IA (jamais de citation dedans non plus) restent inchangés. Nouvelle classe `.clickable-symbol` dans `styles.css`, jumelle de `.clickable-deity` déjà là (même soulignement pointillé doré, juste une couleur de fond différente au clic — data-attribute `data-symbol` plutôt que `data-deity`).
- Deux citations à la formulation irrégulière (une clause descriptive greffée dans la parenthèse plutôt qu'un simple nom, ex. « voir la fiche « Hormos », dont l'autel se trouvait tout près ») ont été réécrites en deux phrases distinctes pour que le mécanisme générique s'applique aussi à elles. Deux citations génériques sans nom précis (« voir les fiches consacrées à chacune », « voir les fiches individuelles », dans la fiche « Muses ») restent, elles, du texte simple : rien à y rendre cliquable.
- En corrigeant ces citations, une coquille préexistante a aussi été repérée dans la fiche « Charites » elle-même (pas seulement Euphrosyne/Aglaé, corrigées juste au-dessus) : elle citait encore « Thalie », la Muse, au lieu de « Thalia », la Charite — corrigée de la même façon.

**"Il faut réécrire la fiche d'Achille. Elle n'est pas clair et surtout très incomplète. Il faut parler de Briseis, de l'amazone qu'il a tué, du peuple des mirmydons, Paris qui la tué, son fils etc."** — l'ancienne fiche (`DEITY_LORE.achille`) tenait en 2 phrases (naissance/talon, puis colère contre Agamemnon jusqu'à la mort d'Hector) : réécrite en 6 paragraphes qui suivent son parcours complet plutôt qu'un résumé télégraphique.
- **Naissance et enfance** : fils de Thétis et du roi mortel Pélée (voir la fiche « Thétis », qui racontait déjà ce mariage), plongé dans le Styx (voir la fiche « Rivière ») pour le rendre invulnérable, sauf au talon. Confié ensuite à Chiron sur le Pélion (voir la fiche « Chiron ») pour son éducation, puis déguisé en jeune fille par Thétis à la cour de Lycomède sur Skyros pour échapper à la prophétie d'une mort glorieuse mais précoce — où il devient pourtant père de Néoptolème avec la princesse Déidamie, avant qu'Ulysse (voir la fiche « Ulysse ») ne le démasque en dissimulant une arme parmi des présents destinés aux filles du palais.
- **Les Myrmidons et Briséis** : le peuple qu'il mène à Troie, les Myrmidons de Phthie — selon la légende, d'anciennes fourmis changées en hommes par Zeus pour repeupler Égine au service de son grand-père Éaque (l'étymologie du nom, *myrmex* = fourmi). La querelle qui ouvre l'Iliade est désormais explicite : Agamemnon, contraint de rendre sa propre captive pour apaiser Apollon, s'empare en représailles de Briséis, la captive d'Achille — l'humiliation précise derrière son retrait des combats, jusque-là seulement décrit comme « une querelle avec Agamemnon ».
- **Patrocle, Hector, Penthésilée** : mort de Patrocle sous les coups d'Hector, duel et mort d'Hector, corps traîné derrière le char, rançon nocturne de Priam à la porte Scée (voir la fiche « Porte », qui racontait déjà cette scène de son point de vue) — puis, nouveau, le duel contre l'Amazone Penthésilée venue prêter main-forte à Troie après Hector, qu'Achille tue et pleure en découvrant sa beauté (même détail déjà présent dans la fiche « Arès », citée en retour).
- **Sa mort et son fils** : mort devant la porte Scée d'une flèche de Pâris guidée par Apollon jusqu'à son talon — jusqu'ici jamais racontée dans sa propre fiche. Néoptolème, cité en passant plus haut (Skyros), reste absent de Troie jusqu'à cette mort ; un oracle l'y appelle ensuite, où il tue Priam au pied d'un autel lors de la prise de la ville — clôture qui met en regard sa dureté et la retenue qu'un deuil avait fini par arracher à son père.
- Note courte (`DEITY_NOTES.achille`, affichée sous son portrait) réécrite dans le même sens : « Le plus grand guerrier de la guerre de Troie, chef des Myrmidons, vulnérable au seul talon que le Styx n'avait jamais pu atteindre » plutôt que la formule vague d'avant (« connu pour sa bravoure et sa colère »). Fiche toujours gratuite (`FREE_FIGURES`), toutes ses citations vérifiées résolues par `linkifyLore()`.

**Comparaison de thèmes — « Mes proches »** (tuile de l'onglet Profil, `showRelations()`) : le même principe que la synastrie en astrologie traditionnelle (croiser deux thèmes plutôt qu'en lire un seul), volontairement généralisé à toute relation — partenaire, enfant, parent, fratrie, ami·e — plutôt que réservé au couple. Chaque proche (`delphesRelations` dans `localStorage`, jamais envoyé au serveur au-delà du calcul du thème via `/api/astral`, exactement comme le profil principal) a son propre thème complet, géré indépendamment (ajout/modification/suppression, `showRelationForm()`) — cette gestion reste **toujours gratuite**, ce n'est que de la donnée, pas de l'interprétation. `compareProfiles()`, entièrement déterministe et local (aucun appel IA), croise ensuite les deux thèmes :
- **Élément dominant** (`SIGN_ELEMENT`, dérivé de la même triplicité que `DECAN_MINOR_CARDS` — pas une nouvelle classification) : même élément = résonance, éléments complémentaires (Feu/Air, Terre/Eau) = équilibre naturel, éléments opposés = tension à apprivoiser plutôt qu'un problème en soi (`elementDynamic()`).
- **Divinité tutélaire commune**, quand `tutelaryDeity()` calculé séparément pour chaque thème tombe sur la même figure.
- **Points communs et points de tension potentiels** (`synastryAspects()`) : Soleil/Lune/Ascendant/Vénus/Mars de chacun comparés deux à deux avec `findAspect()` — les mêmes types et orbes que `CLIENT_ASPECTS` (déjà utilisés pour les aspects natals et pour `strongestTransitAspect()`), ici appliqués entre deux personnes plutôt qu'au sein d'un seul thème ; conjonction/trigone/sextile classés comme points communs, carré/opposition comme tensions potentielles — jamais présentées comme des incompatibilités. Vénus (affinités, valeurs) et Mars (énergie, façon d'agir) sont venus compléter Soleil/Lune/Ascendant après un retour direct ("tu ferais quoi pour pousser encore cette fonction ?") — jusqu'à 5 points par thème, donc jusqu'à 5×5 combinaisons possibles, les 5 plus exacts remontant dans chaque catégorie (common/tense).

Chaque proche déjà enregistré peut être modifié (pas seulement supprimé) depuis « Mes proches » ou directement depuis l'écran de comparaison (`showRelationForm(id)`, préexistant côté logique mais qu'aucun bouton n'exposait avant un retour direct). Le formulaire d'un proche a le même champ **Genre** optionnel que le profil principal (voir plus haut) — sert au même usage, pour que le texte enrichi par IA de la comparaison (plus bas) accorde correctement ce qui concerne le proche.

Pour qu'une lecture répétée ne sonne pas comme un même gabarit recopié pour tout le monde, `elementDynamic()` et `synastryAspects()` piochent chacun parmi plusieurs formulations par cas (`ELEMENT_DYNAMIC_VARIANTS`, `SYNASTRY_ASPECT_VARIANTS`, au moins 3 variantes chacune, plus explicatives que la version initiale — 1 à 2 phrases par variante plutôt qu'un simple fragment). Le choix n'est pas aléatoire : `stableVariant()` (hash djb2) dérive une variante stable à partir de la paire de signes exacte en jeu (12×12 combinaisons pour l'élément, signe+point+type d'aspect pour la comparaison) — une même comparaison affiche donc toujours le même texte d'un rendu à l'autre (pas de scintillement au changement d'écran), tout en donnant à des comparaisons différentes de bonnes chances de tomber sur des formulations différentes. Grammaire : le côté "toi" utilise des groupes nominaux correctement accordés (`SYNASTRY_POINT_YOUR` : "Ta Lune", pas "Ton Lune") et le côté "proche" nomme la personne plutôt que d'utiliser un pronom genré ("la Lune de Léa", jamais "sa Lune").

**Texte enrichi par IA** (`POST /api/comparison-text`, `api/comparison-text.js`) : bonus optionnel au-dessus de la comparaison déterministe ci-dessus (qui reste entièrement fonctionnelle sans lui) — un texte suivi de 2-3 paragraphes qui tisse les résultats déjà calculés (élément, points communs/tensions, divinité commune) en une lecture fluide, avec un ton adapté au type de relation (`RELATION_TONE_HINT` côté serveur : jamais de vocabulaire de couple pour un parent/enfant/fratrie/ami·e, seulement pour un·e partenaire). `profileForComparisonText()` construit le résumé envoyé — jamais les deux thèmes complets, même sobriété que `profileForPortrait()`. Généré une seule fois par proche et mis en cache directement sur l'entrée du proche (`relation.comparisonText` dans `delphesRelations`), régénéré si l'empreinte des dates de naissance des deux thèmes concernés change (`comparisonFingerprint()`, même garde-fou que `astralText.tutelaryDeityKey`) — voir `ensureComparisonText()`, déclenchée uniquement en mode premium (même logique de discrétion qu'`ensurePortrait()` : jamais de prompt de code d'accès surprise, échec silencieux, un seul appel par proche tant qu'aucun texte n'est en cache).

**Fiabilité factuelle du texte généré** : retour direct d'utilisatrice — un texte généré affirmait qu'un fils avait "une Lune en Poissons" alors que c'était son Soleil (sa Lune était en Cancer), malgré des données d'entrée correctes : l'IA avait interverti deux placements en rédigeant. Le prompt de `api/comparison-text.js` présente désormais les données en un **bloc par personne** (`personBlock()` : "Thème de Sophie :" / "Thème de Léa :", chacun listant seulement ses propres placements) plutôt qu'une ligne par planète mélangeant les deux prénoms sur la même ligne — plus explicite pour l'IA — et une consigne d'exactitude explicite ("EXACTITUDE AVANT TOUT… ne les mélange jamais entre les deux personnes… n'invente jamais un placement qui n'est pas listé"). `api/portrait.js` et `api/astral-text.js` reçoivent une consigne équivalente en prévention (même risque de confusion Soleil/Lune en rédigeant, même sans deuxième personne à mélanger). Cela réduit fortement le risque sans pouvoir l'éliminer complètement — une IA générative reste capable d'erreurs occasionnelles même avec des données correctes en entrée ; en cas de doute, les données brutes affichées ailleurs dans l'app (signes, degrés) font foi.

**Comparaison de groupe** (bouton « 👪 Comparer plusieurs proches » dans « Mes proches », visible dès 2 proches enregistrés) : étend la comparaison à deux à un groupe entier — toi et plusieurs proches sélectionnés à la fois, pour voir la dynamique de toute une famille en un coup d'œil plutôt que d'ouvrir une comparaison à la fois. `compareGroup(people)` calcule `compareProfiles()` pour **chaque paire possible** au sein du groupe (C(n,2) paires, pas seulement toi vis-à-vis de chacun — donc aussi, par exemple, deux enfants entre eux) ; volontairement pas de "score de groupe" unique : une famille n'est pas une seule compatibilité mais un réseau de relations à deux qui s'additionnent. Vue condensée par paire (élément dominant, divinité commune, aspect le plus exact) — pour les paires qui ne t'incluent pas, l'aspect est présenté factuellement (type + signes) plutôt qu'avec la phrase personnalisée de `synastryAspects()`, écrite pour s'adresser à toi spécifiquement et donc incorrecte pour une paire entre deux proches. Même découpage gratuit/premium que la comparaison à deux : sélection des proches toujours gratuite, dynamique de groupe elle-même premium.

Fonctionnalité premium (voir plus bas) : le recueil et la gestion des proches restent gratuits, mais la comparaison interprétée elle-même (à deux comme en groupe) est cadenassée sans le mode premium — seuls les signes bruts des deux thèmes restent visibles en aperçu pour une comparaison à deux.

### Découpage gratuit / premium

Toujours porté par le même interrupteur local `isPremiumEnabled()` (rien de tel qu'un vrai paiement — voir « Pistes de suite possibles »), mais depuis cette étape le découpage couvre l'app entière, en vue d'une future publication App Store avec un vrai palier payant (StoreKit). Défini explicitement avec l'utilisatrice ; répertorié ici pour qu'un futur changement de contenu (nouvelle carte, nouveau symbole…) sache dans quel panier tomber par défaut.

**Important — défaut actuel** : `isPremiumEnabled()` renvoie `true` tant qu'aucune valeur n'est stockée (`localStorage.getItem("delphesPremium") !== "0"`), donc **tout le monde a l'accès complet par défaut**, y compris les personnes qui testaient déjà l'app avant l'introduction de ce découpage — aucune manip de leur part. Seul un choix explicite (décocher la case dans l'écran Profil astral, qui écrit `"0"`) fait basculer un appareil en expérience gratuite ; c'est volontairement l'inverse d'un vrai lancement payant. Au moment du vrai lancement App Store, ce défaut devra être inversé (`localStorage.getItem("delphesPremium") === "1"`) pour que le mode gratuit redevienne l'état de repli normal — voir « Pistes de suite possibles ».

- **Toujours gratuit, sans interrupteur** : 1 tirage général par jour (`FREE_SPREAD_TYPES`, `canStartSpread()`, `hasUsedFreeGeneralReadingToday()`/`markFreeGeneralReadingUsed()`, reset à minuit via une simple comparaison de date) ; la carte du jour ; le Journal et ses statistiques ; les données brutes du Profil astral (signes, degrés, maisons — tout sauf les phrases d'interprétation, voir plus bas) ; la « Lecture traditionnelle » (Marseille) de chaque carte, jamais gatée ; noter, modifier et conserver un rêve dans le Journal des rêves (voir « Rêves » plus bas) — ce n'est que de la donnée, comme la gestion des proches.
- **Gratuit, sous-ensemble figé (les *n* premiers dans l'ordre alphabétique d'affichage)** : les 2 premiers arcanes majeurs (`FREE_MAJORS` : Le Mat, I — Le Bateleur), les 4 figures de cour Rois (`FREE_COURTS` : un Roi par enseigne), les 5 premiers symboles de la bibliothèque (`FREE_SYMBOLS`, tri alphabétique `localeCompare(…,"fr")` sur `SYMBOL_LIBRARY[id].label`), les 4 premières figures mythologiques (`FREE_FIGURES`, même tri sur les clés de `DEITY_NOTES`) — `isCardMythFree()`/`isSymbolLoreFree()`/`isFigureLoreFree()` testent l'appartenance à ces ensembles. Les cartes numérales n'ont **jamais** d'éclairage mythologique gratuit (règle explicite, aucune exception).
- **Toujours premium** : tous les autres tirages (Oui/Non, Amour, Croix celtique) ; l'« Éclairage mythologique » des cartes hors des sous-ensembles ci-dessus ; le texte « Aux origines du symbole » et « Le mythe » hors des 5/4 premiers ; dans le Profil astral, le portrait de personnalité et le résumé du thème, et dans « Mythologie personnelle », la divinité tutélaire en entier (section toujours visible mais cadenassée), et toute phrase d'interprétation (nombre du prénom, année/mois personnels, planètes, ascendant, aspects) — les libellés et valeurs brutes restant affichés, seule la phrase explicative est masquée ; la comparaison de thèmes interprétée dans « Mes proches », à deux comme en groupe (élément dominant, points communs/tensions, divinité tutélaire commune, texte enrichi par IA — la gestion des proches elle-même reste gratuite, voir plus haut) ; l'explication IA des arcanes majeurs liés au profil (`majorLinksText`, voir plus haut) ; l'interprétation IA d'un rêve (`/api/dream`, voir « Rêves » plus bas — écrire et conserver le rêve reste gratuit, seule l'interprétation est verrouillée).

Mécanique commune : `premiumLockHTML(message)` rend un bloc 🔒 réutilisable partout où du contenu est masqué ; dans `renderProfilResults()`, un raccourci local `expl(text)` centralise le masquage des phrases d'interprétation (`premiumOn ? texte : ""`) pour ne pas répéter la condition à chaque case. Les sections « Figures liées »/« Cartes concernées » d'un symbole ou d'une figure restent toujours visibles même verrouillé — considérées comme de la navigation, pas de l'« explication ». Pour éviter de gaspiller des appels IA payants sur du contenu qu'un profil gratuit ne verra jamais, `ensurePortrait()`/`ensureAstralText()` ne sont plus appelées (ni depuis `home()`, ni depuis `showProfilAstral()`) tant que `isPremiumEnabled()` est faux.

### Horoscope du jour — comment ça marche
Affiché côté interface sous le titre « Horoscope du jour » ; en interne (fonctions, clé `localStorage`, endpoint `/api/ritual`), le nom garde le préfixe historique `ritual` — pas de renommage en profondeur pour un changement de libellé visible.

`POST /api/transits` (`api/transits.js`) calcule les positions planétaires **du jour** (Soleil à Pluton), sans recevoir aucune donnée personnelle — le résultat est identique pour tout le monde, seul l'instant de la requête compte. Le client (`ensureTransits()` dans `app.js`) met ce résultat en cache un jour entier dans `localStorage` (`delphesTransits`) et ne rappelle l'endpoint qu'une fois par jour.

`strongestTransitAspect()` compare ensuite, entièrement côté client, ces positions du jour au thème natal du Profil astral (déjà en `localStorage`, jamais renvoyé au serveur pour ce calcul) : l'aspect le plus exact entre les planètes rapides du jour (Soleil, Lune, Mercure, Vénus, Mars) et les points natals (Soleil, Lune, Ascendant), avec les mêmes types/orbes que `/api/astral`. Gratuit et instantané (aucun appel IA pour ce calcul) — son résultat, ainsi que les signes du Soleil et de la Lune du jour, alimente `ritualSummary()`, envoyé à `/api/ritual` juste en dessous. Une ancienne section « Horoscope du jour » affichait ces mêmes signes séparément, en plus du rituel : supprimée (elle faisait doublon), sa matière a d'abord été entièrement absorbée dans la recommandation unifiée — avant que son titre d'origine ne lui soit rendu par la suite (voir le paragraphe suivant).

`POST /api/ritual` (`api/ritual.js`) fusionne carte du jour, transits du jour (signes + aspect le plus marqué), **thème natal fixe** (Soleil/Lune/ascendant de naissance) et **nombre personnel du jour** (`personalDayNumber()`, pas du mois) en une seule recommandation concrète, plutôt que des blocs séparés à interpréter soi-même. Le thème natal fixe et le nombre du jour ont été ajoutés après un retour direct : sans eux, deux jours qui partagent la même carte (le cycle ne compte que 22 majeurs, voir `dayOfYear()`) et le même signe solaire transitant (constant tout un mois) produisaient un texte quasi identique — le thème natal distingue une personne d'une autre même à carte/signe du jour identiques, et le nombre personnel change chaque jour (contrairement au mois personnel). Le prompt sépare explicitement dans son texte ce qui est fixe (thème natal) de ce qui varie aujourd'hui, et demande explicitement à l'IA d'éviter toute formule interchangeable d'un jour ou d'une personne à l'autre. Contrairement au portrait et à la rétrospective (générés une fois), rappelé **une fois par jour** — coût récurrent, du même ordre qu'une lecture à 1 carte — et mis en cache dans `localStorage` (`delphesRitual`) le temps de la journée. N'apparaît sur l'accueil que si un Profil astral est enregistré, et jamais via un prompt intrusif : le premier appel n'est déclenché que si le code d'accès a déjà été mémorisé par ailleurs (ce qui est garanti dès qu'un Profil astral existe).

### Journal intelligent — comment ça marche
`journalTrends()` dans `app.js` analyse le Journal (à partir de 3 tirages enregistrés, pour éviter qu'un seul tirage ne « devienne » une tendance) et dégage la carte la plus fréquente et le thème de question le plus fréquent (via `detectDomain()`, déjà utilisé pour la synthèse locale). Affiché en tête du Journal (« Tes tendances »), et envoyé en résumé minimal (`journalTrendsForReading()`) à `/api/reading` pour que la lecture IA puisse, avec beaucoup de subtilité, en tenir compte — jamais au détriment des cartes tirées ce jour-là.

**Affinités mythologiques** (dans Statistiques) : contrairement aux autres fonctionnalités de personnalisation (calculées une fois pour toutes à partir du thème astral), celle-ci se construit et s'affine à mesure que le Journal grossit. `journalStats()` dépouille chaque carte de chaque tirage enregistré et compte, sur l'ensemble du Journal, les divinités (`cardDeityLabel()`, la même fonction que les fiches détail, donc une carte numérale ne compte que si elle a sa propre figure via `NUMBER_CARD_DEITY`) et les symboles (`c[5]`, le même champ qui alimente « Cartes concernées ») qui reviennent le plus souvent — les 5 premiers de chaque, cliquables vers leur fiche complète. Toujours gratuit (fait partie de « le Journal et ses statistiques », jamais gaté par le découpage premium — voir plus haut) et purement local, aucun appel IA.

### Mémoire du Journal — comment ça marche
Contrairement à `journalTrends()` (une tendance globale), `cardMemory()` est un rappel ponctuel propre à chaque tirage : si l'une des cartes tirées aujourd'hui est déjà apparue dans un tirage précédent sur un sujet proche (même domaine détecté par `detectDomain()`), le tirage précédent le plus récent est envoyé à `/api/reading` (`{cardName, date, domainLabel}` — jamais le texte de la question passée) pour que la lecture IA puisse, si c'est pertinent, le mentionner brièvement.

### Rétrospective annuelle — comment ça marche
`POST /api/retrospective` (`api/retrospective.js`) rédige un bilan de l'année écoulée, une fois par an, à l'anniversaire de naissance (repère plus intuitif qu'un 1er janvier arbitraire pour « une année de ta vie »). `ensureRetrospective()` scope le Journal aux 12 derniers mois (`journalStatsSince()`, en s'appuyant sur `journalStats()` généralisé à un sous-ensemble d'entrées), exige au moins 3 tirages sur la période (comme `journalTrends()`), puis envoie un résumé agrégé — jamais le texte des questions ni des notes — à l'API. Le résultat est mis en cache dans `localStorage` (`delphesRetrospective`, avec l'année de génération) et surfacé dans l'onglet Tirage, section « Ton historique », à côté de Journal et Statistiques (voir « Réorganisation demandée » plus haut — cette tuile a d'abord vécu dans Profil, avant ce déplacement vers Tirage) tant que l'année en cours correspond ; une nouvelle rétrospective n'est générée qu'au prochain anniversaire.

## Rêves — comment ça marche
Dans la Grèce antique, l'onirocritique (l'art de décrypter les rêves) était une vraie pratique divinatoire : Artémidore de Daldis en a laissé, au IIe siècle après J.-C., le traité le plus complet de l'Antiquité, l'*Onirocriticon*. Cette section s'en inspire — barre du bas → **Rêves** (`reves()` dans `app.js`, route `"reves"`), phrase d'intro terminée par « Raconte ce dont tu te souviens, pour en connaître la signification. » (retour direct d'utilisatrice), deux tuiles :

- **Noter un rêve** (`showDreamForm()`/`renderDreamForm()`) : un simple champ de texte libre (`dreamState.text`, brouillon persistant dans `localStorage` sous `delphesDreamState` — comme `tirageState` pour un tirage en cours) avec un bouton « Enregistrer dans mon journal », toujours disponible et gratuit. Un second bouton, « Interpréter ce rêve » (n'apparaît qu'en mode premium — voir « Découpage gratuit / premium » plus haut), envoie le récit — et, s'il est renseigné, le contexte de vie de la personne (`profileForDream()`, voir « Contexte de vie » plus haut : le métier notamment change beaucoup le sens plausible d'un rêve) ainsi que le tirage de tarot le plus récent (`recentReadingForDream()`, voir « Rêves et tirages : une lecture croisée » ci-dessous) — à `POST /api/dream` (`api/dream.js`), qui répond avec `{ analysis }` : 2 à 4 paragraphes qui identifient les éléments marquants du récit et proposent un sens concret, jamais une prédiction littérale de l'avenir (contrainte explicite du prompt — cette fonctionnalité s'inspire de l'introspection symbolique des Anciens, pas de la voyance événementielle). Si le rêve est déjà enregistré, l'interprétation obtenue vient mettre à jour l'entrée existante plutôt que d'en créer une nouvelle.
- **Journal des rêves** (`showDreamJournal()`/`dreamJournalView()`) : liste de tous les rêves enregistrés (`dreams` dans `localStorage`, clé `delphesDreams` — une archive séparée du Journal des tirages), avec la même mécanique `<details>`/« Modifier »/« Supprimer » que le Journal des tirages. Une interprétation déjà générée reste visible tant que le mode premium est actif ; si l'interrupteur est désactivé après coup, elle se cache derrière une invite à réactiver le mode premium plutôt que de disparaître — cohérent avec le reste du découpage gratuit/premium de l'app (basé sur l'état courant de l'interrupteur, pas sur "déjà généré une fois").

**Vie privée** : comme pour un tirage, le récit du rêve n'est envoyé à l'API qu'au moment de la demande d'interprétation (fonction serverless sans état, rien n'est journalisé ni persisté côté serveur) — l'archive elle-même reste 100% locale, jamais synchronisée. Même protection optionnelle `APP_ACCESS_CODE` que les autres endpoints IA.

**Note d'architecture** : cette section a pris la place de l'ancien onglet "Symboles" dans la barre de navigation — pour ne pas ajouter un 6e onglet, la bibliothèque symbolique a été fusionnée dans **Apprendre** comme une tuile de plus (« Bibliothèque symbolique », `showSymbolsLibrary()`, anciennement la fonction `symboles()` de premier niveau), au même titre que les Arcanes majeurs/Figures de cour/Cartes numérales/Figures mythologiques déjà présentes.

**Illustration** (`revesHeroHTML()`, fournie par l'utilisatrice — `assets/reves-hero.jpg`, 700×1050) : affichée juste après l'emblème et le titre de l'écran Rêves (retour direct d'utilisatrice : elle passait avant le titre, seul onglet à rompre l'ordre emblème → titre → contenu suivi partout ailleurs — corrigé pour une vraie unité visuelle), sous forme "vivante" comme les vignettes de cartes (`ken-burns`, mais plus lente et douce — 16s, moins de zoom — pour une impression de nuages qui dérivent plutôt qu'un zoom marqué) avec quelques étoiles scintillantes par-dessus (classe `.star`, déjà utilisée par `starLoaderHTML()` — même scintillement `twinkle`, juste repositionnées à la main sur l'image). Respecte `prefers-reduced-motion` comme le reste des animations de l'app.

**Rêves et tirages : une lecture croisée** — retour direct d'utilisatrice ("pour les tirages de tarot, l'IA va aussi rechercher dans les rêves [...] et vice versa ?") : les deux endpoints IA reçoivent chacun, en plus de leur propre contenu, un court indice OPTIONNEL sur l'autre pratique, à utiliser seulement si c'est vraiment pertinent — jamais un rapprochement forcé entre deux choses sans rapport :
- `recentDreamForReading()` (dans `app.js`) résume le rêve le plus RÉCENT (`dreams[0]`, trié du plus récent au plus ancien) — mais seulement s'il date de moins de `RECENT_DREAM_MAX_DAYS` (7) jours, sinon `null` : un rêve trop ancien n'a plus grand-chose à voir avec la question posée aujourd'hui. La fraîcheur se calcule sur `id` (= `Date.now()` à la création du rêve, jamais recalculé sur une modification), `date` n'étant qu'une chaîne déjà formatée (`toLocaleString`), pas exploitable pour un calcul d'ancienneté. N'envoie qu'un court extrait (même troncature que `dreamJournalView()`), jamais le récit complet. `generateAIReading()` l'ajoute au corps de `POST /api/reading` (champ `dream`), qui le tisse dans le prompt comme un indice à côté de `history`/`memory` (Journal des tirages) déjà existants — même discrétion, jamais mentionné si ça n'apporte rien.
- `recentReadingForDream()` (dans `app.js`), le sens inverse : résume le tirage le plus RÉCENT (`journal[0]`) — les cartes tirées et un court extrait de la synthèse déjà générée, mais **jamais la question posée** (même discrétion vie privée que `cardMemory()`). Pas de filtre de fraîcheur ici (contrairement aux rêves, une entrée du Journal des tirages n'a pas de repère numérique exploitable pour ça) : le tirage le plus récent reste digne d'être mentionné même après un délai. `generateDreamAnalysis()` l'ajoute au corps de `POST /api/dream` (champ `recentReading`).

Les deux prompts sont explicites sur le caractère facultatif de cet indice ("INDICE OPTIONNEL seulement [...] sinon ignore-le complètement, ne force jamais un rapprochement artificiel") : la plupart des lectures/interprétations n'auront donc aucune raison de s'y référer, et c'est voulu — ce n'est qu'une porte ouverte pour les cas où le lien saute vraiment aux yeux.

## Déployer sur Vercel

Vercel héberge en un seul projet le site statique (racine du repo) et la fonction backend (`api/reading.js`) — c'est l'option la plus simple pour ce projet.

1. Créer une clé API sur [console.anthropic.com](https://console.anthropic.com) (nécessite un moyen de paiement configuré) et, si souhaité, y fixer un plafond de dépense mensuel (Settings → Limits).
2. Sur [vercel.com](https://vercel.com), **New Project** → importer ce dépôt GitHub.
3. Aucune configuration de build n'est nécessaire (site statique + dossier `api/` détectés automatiquement).
4. Dans **Settings → Environment Variables**, ajouter :
   - `ANTHROPIC_API_KEY` = la clé créée à l'étape 1 (cocher Production, Preview et Development).
   - `APP_ACCESS_CODE` = un code secret de ton choix (voir ci-dessus ; optionnel mais recommandé tant que l'appli est personnelle).
5. Déployer. Vercel donne une URL du type `https://ton-projet.vercel.app/`.

Pour tester en local avec la CLI Vercel :
```bash
npm install
cp .env.example .env   # puis renseigner ANTHROPIC_API_KEY (et APP_ACCESS_CODE si souhaité)
npx vercel dev
```

*(Netlify fonctionne aussi selon un principe identique : Netlify Functions à la place du dossier `api/`, et la même variable d'environnement dans Site settings → Environment variables — mais nécessiterait d'adapter `api/reading.js` au format Netlify Functions.)*

## Installer sur iPhone une fois déployé
1. Ouvrir l'URL de déploiement (Vercel) dans Safari sur l'iPhone.
2. Appuyer sur Partager → Ajouter à l'écran d'accueil → Ajouter.
3. L'application apparaît avec son icône et s'ouvre comme une app, y compris hors ligne (sauf la lecture IA, qui a besoin du réseau — avec repli automatique sinon).

## Politique de confidentialité (avant publication sur un store)
Apple (App Store Connect) et Google (Play Console) exigent tous les deux une URL de politique de confidentialité au moment de la soumission — même pour une appli qui, comme celle-ci aujourd'hui, ne crée pas de compte et ne fait remonter aucune donnée à un serveur pour la conserver : le simple fait d'envoyer des données à un tiers pour traitement (ici, un résumé du profil à l'API Anthropic pour générer une lecture IA) doit être déclaré.

`politique-confidentialite.html` est une page statique et **autonome** (aucune dépendance à `app.js`/`styles.css` — juste ses propres styles inline, pour rester consultable même si le reste de l'appli change de forme) déployée au même endroit que le reste de l'appli, donc accessible à une URL stable du type `https://<ton-domaine-vercel>/politique-confidentialite.html`. C'est cette URL qu'il faut coller dans le champ « Privacy Policy URL » d'App Store Connect / Play Console. Un lien discret vers cette même page est aussi présent en bas de l'onglet Profil (`profil()` dans `app.js`) — Apple demande généralement qu'elle reste accessible aussi bien depuis l'appli que depuis la fiche du store.

**Deux champs restent à compléter avant publication** (marqués `[...]` directement dans le fichier) : le nom (ou la structure) qui exploite l'appli, et une adresse e-mail de contact pour les questions de confidentialité — volontairement laissés en placeholder plutôt que pré-remplis avec une adresse personnelle sans confirmation explicite, cette page étant vouée à devenir publique et durable.

**Cette page ne décrit que les pratiques RÉELLES et ACTUELLES de l'appli** (rien n'est stocké sur un serveur, aucun compte, aucune mesure d'audience) — volontairement, plutôt que d'anticiper des fonctionnalités pas encore construites. Le jour où l'appli gagne des comptes utilisateurs, une sauvegarde en ligne, ou des statistiques d'usage (voir « Pistes de suite possibles » plus bas), cette page devra être mise à jour AVANT la mise en service de ces fonctionnalités, pas après — une politique de confidentialité doit toujours refléter la pratique en vigueur, jamais une pratique passée ou anticipée. Sa date de dernière mise à jour, en haut de la page, doit être rafraîchie à chaque changement de fond.

## Prête pour l'App Store ?
Question posée directement par l'utilisatrice ("Pour toi, l'application est elle prête à basculer sur l'app store ?"). Réponse honnête à ce stade : pas encore — pas à cause du contenu (78 cartes illustrées, lecture IA, découpage gratuit/premium bien défini, offline géré proprement), mais pour des raisons structurelles :

- **Blocage principal, spécifique à Apple** : l'app n'existe aujourd'hui QUE comme PWA (installée via Safari « Ajouter à l'écran d'accueil »). Apple n'accepte pas ça comme soumission à l'App Store — il faut un vrai binaire construit via Xcode. Ce n'est pas un réglage à changer, c'est un chantier entier : un emballage natif (Capacitor, déjà noté dans « Pistes de suite possibles ») qui embarque cette même app dans une coquille native. Sans ça, il n'y a rien à soumettre à Apple. *(Google Play est plus direct : Android accepte les PWA via une TWA — Trusted Web Activity — un emballage plus léger.)*
- **Paiement réel** : géré (voir « Paiement premium » plus haut) — la brique Stripe est construite et testée, seulement pas encore activée (`PAYMENT_ENABLED = false`). Reste, le jour de l'activation : créer le compte Stripe, le Price à 9,99€/mois, configurer les variables d'environnement, et tester un vrai paiement de bout en bout.
- **Garde-fou sur les coûts IA** : géré (voir « Garde-fou sur les appels IA » plus haut) — plafond de 50 appels IA par personne et par mois en premium, pour éviter une facture qui grimpe sans qu'on s'en aperçoive pendant cette phase de test.
- **Politique de confidentialité** : deux champs encore en placeholder (nom/structure + e-mail de contact, voir « Politique de confidentialité » plus haut) — rapide à corriger, mais obligatoire avant soumission.
- **Deux points mineurs, à vérifier plutôt qu'à construire** : l'app n'a été testée que sur ordinateur (Playwright/Chromium) de mon côté ; l'utilisatrice confirme de son côté que les deux personnes qui testent le font depuis leur téléphone et que ça fonctionne bien — un test explicite sur les deux plateformes (iOS/Safari et Android/Chrome) avant soumission reste une bonne pratique, les moteurs de rendu pouvant avoir des différences subtiles (notamment côté PWA/offline). L'app est entièrement en français ; l'App Store demande a minima une fiche produit en anglais, même si le contenu de l'app reste en français.

**Ordre logique pour avancer** : compléter la politique de confidentialité → décider si le premium doit vraiment rapporter avant de créer le compte Stripe et d'activer `PAYMENT_ENABLED` → emballage natif (Capacitor) → test sur appareils réels des deux plateformes → soumission.

## État actuel du reste de l'app
- Logo de marque (clé + soleil + étoile, `assets/logo-mark.png`) dans le coin haut-gauche de la topbar de chaque écran, à la place de l'ancien bouton retour (`#backBtn`, entièrement retiré) ; boutons Précédente/Suivante sur les fiches de carte parcourues depuis une grille de l'onglet Apprendre ; navigation retour désormais uniquement via « ← Retour » (`#detailBack`, sur chaque écran non-racine), les onglets du bas et `#homeBtn` — voir « Filigrane de marque, navigation Précédente/Suivante… » plus haut
- Icône d'application (installation PWA, écran d'accueil iOS/Android) : le même logo de marque, sur un vrai carré aux coins arrondis rempli de bord à bord — voir « Icône d'application » plus haut
- Navigation 5 onglets (Accueil / Tirage / Apprendre / Rêves / Profil-Astro) — Symboles n'est plus un onglet de premier niveau depuis l'ajout de Rêves, fusionné dans Apprendre comme une tuile de plus (voir « Rêves — comment ça marche »). Retour direct d'utilisatrice : la tuile « Apprendre » de l'accueil (`home()` dans `app.js`) disait encore « Un parcours progressif sur les 78 cartes », un texte antérieur à l'ajout des figures mythologiques et de la bibliothèque symbolique dans cet onglet — corrigé pour refléter les trois catégories réellement explorables (cartes, figures mythologiques, symboles). 5e onglet renommé « Astro » dans la barre du bas (route interne restée `"profil"`, voir « Réorganisation demandée » plus haut).
- Tirage : 4 types de tirage au choix (`SPREADS` dans `app.js`), écran de sélection animé (halo scintillant + pluie d'étoiles) — Tirage général (3 cartes, pioche de 15), Oui/Non (1 carte, pioche de 9), Amour (3 cartes — Toi / L'autre / La relation, pioche de 15), Croix celtique (10 cartes avec position dédiée pour chacune, pioche de 24). Bouton « ← Changer de tirage » pour revenir à la sélection. Lecture personnalisée générée par IA (tient compte de la position de chaque carte dans le tirage), avec animation de chargement (étoiles dorées scintillantes) et repli local si l'IA ne répond pas, enrichie par le Profil astral quand il existe. Sous la grille de tirages, une section « Ton historique » regroupe Journal, Statistiques et (quand elle est prête) la Rétrospective annuelle — déplacés ici depuis l'onglet Profil/Astro (voir « Réorganisation demandée » plus haut). Un 5e type de tirage, « Année à venir » (une carte par mois restant de l'année), a existé puis a été retiré du choix proposé — retour direct d'utilisatrice. Retour direct d'utilisatrice antérieur : la tuile « Tirer les cartes » de l'accueil annonçait encore "trois cartes" (`home()` dans `app.js`) alors que plusieurs types de tirage existent, de 1 à 10 cartes selon le choix — texte corrigé pour ne plus annoncer de nombre de cartes fixe (« Pose ta question, choisis le tirage qui lui correspond »), et volontairement générique pour ne plus se périmer à l'ajout d'un futur type de tirage
- Bibliothèque de 100 symboles et nombres, triés par ordre alphabétique, tous cliquables et reliés aux cartes/divinités ; position de scroll préservée à la navigation. Sa tuile dans l'onglet Apprendre (`apprendre()`) a désormais sa propre illustration (`assets/learn-symboles.jpg`, fournie par l'utilisatrice, même format 500×500 que les 4 autres tuiles de cet écran) via `illusHTML()` — elle n'en avait aucune jusque-là, contrairement aux autres catégories. Les 90 symboles (plantes, animaux, objets, attributs des dieux, lieux, éléments, astres…) ont tous en plus un texte mythologique détaillé (`lore`, plusieurs paragraphes) affiché dans leur fiche sous « Aux origines du symbole ». La section « Cartes concernées » de chaque fiche (`cardsForSymbol()`) s'appuie sur le champ `symbols` (c[5]) de chaque carte — longtemps incomplet pour deux raisons structurelles, corrigées depuis : les figures de cour n'avaient jamais ce champ (toujours vide, `COURTS` n'en avait pas la place — 5e élément ajouté par figure quand son mythe le justifie) et les cartes numérales le partageaient entre toutes les cartes du même rang ou de la même enseigne (`NUMBER_KEYS`/`SUITS`), rendant impossible un lien propre à une seule carte comme "2 de Bâtons" (Artémis) — voir `NUMBER_CARD_SYMBOLS`, même principe que `NUMBER_CARD_DEITY`, pour les cartes numérales illustrées qui en ont besoin
- Figures mythologiques (onglet Apprendre → « Figures mythologiques », `DEITY_NOTES`) : 111 divinités et héros (nombre affiché dynamiquement dans l'app via `Object.keys(DEITY_NOTES).length`, à ne pas confondre avec ce chiffre figé ici — susceptible de dater dès la prochaine addition) ont chacun leur fiche dédiée — 78 d'entre eux sont réellement incarnés par une carte du jeu (`deityFigureHasCard()`), les 33 autres n'existant que comme contexte mythologique cité dans le `lore` d'une autre fiche (voir « Dans la tuile figure mythologique… » plus haut). Elles ont en plus un texte mythologique développé (`DEITY_LORE`, séparé de `DEITY_NOTES` pour ne pas changer son type ailleurs dans le code) affiché sous « Le mythe » — même traitement que les symboles, avec renvois croisés entre fiches (ex. la fiche « Iris » renvoie à « Arc-en-ciel », et inversement). Recherche en direct (`#figuresSearch`) sur cette liste, même mécanisme que la recherche des Symboles mais câblé directement dans `showLearnFigures()` (cet écran ne passe pas par `bind()`)
- Détail de carte enrichi : lecture Tarot de Marseille + éclairage mythologique pour les 22 arcanes majeurs
- Profil/Astro : Profil astral (thème natal complet + numérologie du prénom et du temps), en localStorage. Journal des tirages passés (tuile de l'onglet Tirage désormais, voir « Réorganisation demandée » plus haut, mais toujours en localStorage) : garde la lecture complète (texte par carte + synthèse, IA ou générée hors-ligne), pas seulement les noms des cartes — repliable sous « Revoir la lecture complète » sur chaque entrée. Ré-enregistrer un tirage déjà sauvegardé met à jour l'entrée existante plutôt que d'en créer une en double. Une fois 3 tirages enregistrés, un bandeau « Tes tendances » dégage la carte et le thème de question les plus fréquents (voir « Journal intelligent » plus haut)
- « Mythologie personnelle » (tuile de l'onglet Profil/Astro, n'apparaît que si un Profil astral est enregistré) met en avant les arcanes majeurs liés au thème (Soleil/Lune/Ascendant, via `ZODIAC_MAJOR_LINKS`, tradition ésotérique classique), aux côtés de la divinité tutélaire — voir « Mythologie personnelle » plus haut
- Bracelet personnalisé : mécanisme entièrement construit (recette gratuite à partir des arcanes majeurs liés, réduction premium, lien de transfert vers la future boutique) mais **masqué pour l'instant** (`BRACELET_ENABLED = false`) — voir « Bracelet personnalisé » et « Enlève les boutons faire son bracelet... » plus haut
- Paiement premium (Stripe, 9,99€/mois) : mécanisme entièrement construit et testé (checkout, confirmation, revérification quotidienne du statut d'abonnement) mais **inactif pour l'instant** (`PAYMENT_ENABLED = false`) — voir « Paiement premium » plus haut. Garde-fou de 50 appels IA par personne et par mois en premium (`AI_CALL_LIMIT_PER_PERSON`), lui, actif dès aujourd'hui — voir « Garde-fou sur les appels IA » plus haut
- Statistiques (onglet Tirage → Statistiques, `journalStats()`) : série de jours actuelle et meilleure série jamais atteinte, répartition majeurs/mineurs tirés, cartes qui reviennent le plus souvent, nombre de cartes différentes déjà croisées sur les 78 du jeu, répartition par thème de question et par type de tirage. Contrairement aux « tendances » du Journal (qui exigent 3 tirages avant de s'afficher), ces chiffres apparaissent dès le premier tirage enregistré
- Progression personnelle (onglet Apprendre) : score composite (`learningProgress()`) sur ce qui a été réellement **consulté en détail**, pas seulement tiré — 60% cartes (78), 20% figures mythologiques (70), 20% symboles et nombres (95), chaque fiche comptée une seule fois même revisitée (`markSeen()`/`seenCount()`, `localStorage`)
- Animations : flip 3D dos → face au choix d'une carte à la pioche (`cardBackHTML()`/`bindDeck()` — la face n'est chargée qu'au clic, jamais préchargée pour les cartes jamais choisies), pluie de particules dorées à ce moment-là et ripple doré sur les boutons (`spawnGoldBurst()`, écouteur délégué en fin d'`app.js`), transitions d'écran différenciées (léger zoom avant en entrant dans une fiche, zoom arrière au retour/changement d'onglet — `triggerScreenAnim(kind)`), lecture d'ensemble d'un tirage qui s'écrit mot par mot façon oracle (`typewriterHTML()`, durée plafonnée quelle que soit la longueur du texte), flash « Première découverte » sur une fiche jamais consultée avant (`discoveryFX()`), halo de fond qui varie doucement selon l'heure du jour, et filigrane de constellation derrière le titre de **chacun des 5 onglets** (`constellationHTML(variant)`, `CONSTELLATION_VARIANTS` — retour direct d'utilisatrice : auparavant limité à Apprendre seulement, et jugé trop discret). Deux passes successives sur ce filigrane, la première jugée insuffisante : un motif { `stars`, `edges` } par onglet plutôt qu'un unique chemin séquentiel — `edges` relie certaines étoiles entre elles (une même étoile peut apparaître dans plusieurs paires, formant une vraie jonction) tandis que d'autres restent isolées (poussière d'étoile sans trait), pour vraiment "faire constellation" plutôt qu'un simple gribouillis point à point. Intensité volontairement inégale (`CONSTELLATION_STAR_OPACITY`/`CONSTELLATION_EDGE_OPACITY`, plusieurs paliers plutôt qu'un aplat uniforme) et léger halo doré (`filter:drop-shadow`) sur chaque étoile pour un rendu plus féerique que graphique. Le tracé se dessine trait par trait, chacun avec sa propre durée/délai (`stroke-dashoffset`, ~1.5s par trait, décalés d'~1s) — sensiblement plus lent que la première version (tout le motif en 2.2s, jugée "beaucoup trop rapide") — et chaque étoile s'allume quand le premier trait qui la touche l'atteint (`constellation-pop`, via la variable CSS `--star-opacity` pour ne pas écraser son intensité propre). `.hero`/`.draw-zone` portent désormais explicitement `z-index:0` en plus de `position:relative`, pour isoler leur propre contexte d'empilement : sans ça, la constellation de l'onglet Tirage (`.draw-zone`) pouvait se peindre par-dessus la grille de tirages juste en dessous plutôt que de rester confinée derrière son titre — le bug précis remonté par l'utilisatrice. (Symboles, désormais une simple tuile d'Apprendre plutôt que son propre onglet avec hero, n'a pas son propre motif — comme les autres sous-écrans d'Apprendre : Figures de cour, Cartes numérales, Figures mythologiques, qui héritent visuellement de l'écran Apprendre). Tout respecte `prefers-reduced-motion` (le tracé et les étoiles restent visibles, avec leur intensité propre, juste sans l'animation qui les dessine)
- Ambiance sonore par onglet (`AMBIENT_MOODS`/`AmbientAudio` dans `app.js`) : retour direct d'utilisatrice — une nappe musicale générative (Web Audio API, aucun fichier audio) accompagne chacun des 5 onglets, avec sa propre couleur (accord de 4 voix, filtre, tempo de respiration) — chaleureuse pour Accueil, plus feutrée/mystérieuse pour Tirage, claire pour Apprendre, très douce et lente pour Rêves, ouverte et scintillante pour Profil. Les 4 voix persistent et glissent en douceur d'un accord à l'autre au changement d'onglet (`setMood()`, `setTargetAtTime`) plutôt que de couper puis relancer. Anti-répétition (retour direct d'utilisatrice — "ça prend vite la tête" à rester longtemps sur le même onglet) : fondamentale/quinte restent fixes comme ancrage, mais les 2 autres voix errent lentement parmi une palette de notes propre à chaque onglet (`scheduleWander()`, délai aléatoire 25-55s par voix, jamais synchronisées) ; les 4 voix respirent à des vitesses légèrement décalées plutôt qu'en même temps (`AMBIENT_VOICE_RATE_MUL`, pour ne jamais boucler exactement, même principe que les boucles de bande d'Eno) ; le filtre respire lui aussi lentement (~32s) ; et un panoramique stéréo dérive doucement par voix (`StereoPannerNode`, quand disponible) pour donner de la largeur plutôt qu'un point sonore figé. Muette par défaut — les navigateurs bloquent de toute façon tout son avant un vrai geste, et une appli de tarot doit pouvoir s'utiliser en silence sans surprise — activable via le bouton flottant `#soundBtn` (icône 🔈/🔊, halo doré quand actif), préférence mémorisée en `localStorage` (`delphesSoundMuted`). Choix délibéré de génératif plutôt que de vrais fichiers : aucun souci de licence/droits, poids quasi nul pour la PWA, et une couleur sonore par onglet sans multiplier les fichiers — voir « Pistes de suite possibles » pour la suite (sons d'interaction, vrais morceaux en remplacement à la carte).
- Carte du jour enrichie (accueil) : répartie sur les 365/366 jours de l'année (au lieu des 1-31 jours du mois, qui faisait retomber le 1er de chaque mois sur la même carte), avec un extrait de sa lecture mythologique affiché directement, un signalement si elle résonne avec le profil astral enregistré, et une **série de jours consécutifs** (`updateStreak()`, `localStorage`) qui s'incrémente à la première visite de chaque jour
- Calendrier attique (accueil) : date du jour dans le calendrier civil d'Athènes antique (`atticCalendarInfo()`), calculée entièrement côté client par arithmétique (nouvelle lune moyenne + solstice de juin approximé par la formule basse précision de Meeus, aucun appel réseau) — affiche le mois et le jour, et, quand ce jour précis du mois a traditionnellement une divinité qui lui est propre (la Tétrade du 4 pour Hermès/Aphrodite/Héraclès/Éros, le 6 pour Artémis, le 7 et le 20 pour Apollon, le 8 pour Poséidon, le dernier jour du mois pour le Deipnon d'Hécate), une puce cliquable vers sa fiche. Approximatif par nature (voir le commentaire en tête du bloc dans `app.js`), à la mesure d'un calendrier qui, dans l'Antiquité, dépendait lui-même de l'observation directe du ciel
- Horoscope du jour (accueil, si un Profil astral est enregistré) : une recommandation unique par IA qui relie carte du jour, transits du jour (signes + aspect le plus marqué avec le thème natal, `strongestTransitAspect()`), thème natal fixe (Soleil/Lune/ascendant de naissance) et nombre personnel du jour (voir « Horoscope du jour » plus haut) — seul appel IA récurrent (quotidien) de l'appli
- Rétrospective annuelle (onglet Tirage, section « Ton historique », à l'anniversaire) : bilan par IA de l'année de tirages écoulée, dès 3 tirages enregistrés sur les 12 derniers mois (voir « Rétrospective annuelle » plus haut)
- Mémoire du Journal : une lecture IA remarque si une carte tirée aujourd'hui l'a déjà été pour un sujet proche (voir « Mémoire du Journal » plus haut)
- Accueil personnalisé (« Bonjour {prénom} ✦ ») dès qu'un Profil astral est enregistré ; les lectures générées par IA sont aussi invitées à s'adresser à la personne par son prénom, ponctuellement
- Divinité tutélaire (avec l'explication de ce choix, désormais dans l'écran « Mythologie personnelle »), portrait de personnalité et textes astraux personnalisés par case (nombre du prénom, chaque planète, l'ascendant, chaque aspect), ces derniers restés dans le Profil astral, tous rédigés par IA (voir « Profil astral » et « Mythologie personnelle » plus haut)

## Pistes de suite possibles (non bloquantes)
- ~~Ajouter les illustrations des cartes mineures restantes~~ — fait : les 78 cartes du jeu (22 arcanes majeurs + 56 mineures, Épées/Bâtons/Coupes/Deniers) sont désormais toutes illustrées (même gabarit, 500×750px, JPEG qualité ~88-90), Deniers ayant terminé son rattrapage en 3 lots successifs fournis par l'utilisatrice. La double lecture Marseille + mythologie (`CARD_LORE`) couvrait déjà les 78 cartes ; illustration et texte se rejoignent donc désormais complètement.
- Système de compte / sync cloud si l'app doit devenir multi-appareil (actuellement tout est en localStorage, donc local à l'appareil)
- ~~Vrai système premium~~ — la brique paiement (Stripe, 9,99€/mois) est construite et testée, voir « Paiement premium » plus haut ; reste inactive (`PAYMENT_ENABLED = false`) en attendant que l'utilisatrice soit prête à l'activer (compte Stripe, Price, variables d'environnement). Apple/Google IAP (StoreKit/Play Billing) restent à ajouter séparément le jour de l'emballage natif (voir juste en dessous) — Stripe seul ne suffit pas pour une publication App Store, Apple exige son propre système d'achat pour du contenu numérique consommé dans l'app.
- Emballage en app native (Capacitor) pour publication App Store / Google Play — voir « Prête pour l'App Store ? » plus haut : blocage principal actuel, l'app n'existe aujourd'hui QUE comme PWA, or Apple n'accepte pas ça comme soumission
- ~~Suivi des coûts d'API si l'usage grandit~~ — un premier garde-fou existe (`AI_CALL_LIMIT_PER_PERSON = 50`, voir « Garde-fou sur les appels IA » plus haut), pensé pour une phase de test avec des personnes connues ; une vraie protection contre un usage massif/malveillant (identité serveur, quotas par vrai compte plutôt que par appareil) restera nécessaire si l'app s'ouvre à un public plus large
- Suite de l'ambiance sonore (voir « Ambiance sonore par onglet » plus haut) : sons d'interaction ciblés (cartes battues au tirage, chime doux à l'interprétation d'un rêve), et remplacement à la carte de la nappe générative d'un onglet par un vrai morceau si l'utilisatrice trouve des fichiers correctement libres de droits pour tel ou tel onglet
- Traduction complète EN/ES : nécessite d'abord un vrai système d'i18n (aucun aujourd'hui, tout le texte français est en dur dans `app.js`) avant de traduire le contenu éditorial (78 fiches `CARD_LORE`, 111 `DEITY_LORE`, ~90 `SYMBOL_LIBRARY`, interface) — le contenu généré par IA (`/api/*`) peut lui être localisé directement via le prompt, sans ce chantier préalable
- Boutique de bijoux personnalisés (bracelet généré à partir des arcanes majeurs liés, voir « Bracelet personnalisé » plus haut) : la partie app (recette, lien de transfert, réduction premium) est faite mais volontairement masquée pour l'instant (`BRACELET_ENABLED = false`, voir « Enlève les boutons faire son bracelet... » plus haut) ; reste à créer la boutique elle-même (`BRACELET_SHOP_BASE_URL`/`BRACELET_PREMIUM_DISCOUNT_CODE` sont des placeholders) et sa capacité de fabrication réelle derrière chaque recette. Prochaines briques probables : décoder les paramètres `cards`/`for`/`promo` côté boutique pour pré-remplir la configuration du bracelet (charms/couleurs par arcane, à définir avec le fabricant), appliquer la réduction si `promo` est présent, puis repasser `BRACELET_ENABLED` à `true`.
